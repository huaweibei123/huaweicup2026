# v8作者与排版接口（用户最新：代码式伪代码）

Gemini只写Markdown逻辑；Codex核实固定源码、排版。用户要求示意图后紧接真实源码对应的伪代码，使用代码缩进、赋值、if/for/return/try/except；中文解释作为 # 注释。不要再用编号长句冒充代码。格式：

::: algorithm {#alg:unique title="简短中文标题"}
```python
# 输入：正文已定义的对象
# 输出：实际结果
result = source_function(inputs)  # 简短解释
if condition:
    return result
```

**源码对应：** 固定提交简称，路径，函数。
:::

Python风格伪代码不宣称可直接运行；允许有明确定义的抽象辅助过程。保留真实分支顺序、阈值、预算、异常与返回；不得用新抽象过程隐藏关键判断。局部过程宜18–24行，完整入口宜24–34行（含注释），短行约70个ASCII字符或35中文字符，不使用列表段落或长串数学TeX。对应符号用可读变量名，数学关系放相邻正文。复杂输入可用两行注释。代码中的下划线不转义，不放美元数学标记。

算法跟随对应图片，现有总体算法只保留一次；图文避免重复铺陈。局部步骤标明仅覆盖哪个函数，不伪装完整求解器。case_026图X导出、Y保留、J汇合，对应关系不可交换。算法编号、颜色、行号、边框由Codex渲染，不手写LaTeX。
