# 检查点核验收据

日期：2026-09-26。阶段：匿名完整 PDF 检查点 01。**可通读整稿；尚未完成独立科学审读和最终提交验收。**

## 已完成

- 用户本轮16项要求逐项写入 `DETAILED_OUTLINE_AND_ALIGNMENT.md`；定义已确认要求与拟定组织方案的区别。
- 摘要、第1～8章、参考文献和附录形成连续文字；包含局部无环、超图费用/割守卫、树排序交换、未来充分性与补充必要界推导。
- 18个图稿说明已嵌入相应论证段落，描述真实数据绑定、三问统一风格、不同切法与overlap对照、工具方案及验收。
- 已实际重读三问冻结入口和关键构造，收到三位研发作者的固定版本确认；Coherent综合原件和定义修订建议已读取，未把修订建议冒充全队已采纳协议。
- `export_tables.py`静态导出1500行，精确覆盖每题100×5；15个均值与冻结报告一致；原始单核分母SHA及身份、P3同计划无L2原件SHA及身份通过；与R9F最终CSV的Forest/最终M和字节命中率一致。
- 数据导出读取822个固定Git对象，来源、SHA和大小保存在`data/source-receipt.json`。读取对象数不是全部计划重新审计数。
- 本轮新solver/E0/E1/E2调用均为0；未发起新的Pro研究，未创建子Agent。
- 本机现有Manim Community 0.20.1命令和import通过；ffmpeg/TeX入口存在。未渲染场景，未测试图像或动画成品。
- 外部参考文献按出版方/作者入口核对。Sethi–Ullman元数据由作者目录及JACM卷目录交叉核对，ACM直接读取403；未声称阅读全文。

## 实际命令

```sh
python3 paper/manuscript-v1/scripts/export_tables.py
python3 paper/manuscript-v1/scripts/assemble_markdown.py
PAPER_LAYOUT=1 python3 paper/manuscript-v1/scripts/draw_figures.py
python3 paper/manuscript-v1/scripts/build_checkpoint.py
git diff --check
```

已执行冻结数据绘图、Markdown 到 LaTeX 转换与 XeLaTeX 两遍构建。图号、图名、图注由 LaTeX 管理；双栏超图机制图采用原生透明 PNG。原模板类文件未修改；原作者章节、算法和图目录未覆盖。

## 本次 PDF 复核

- 全文与附表分开；正文包含全部三题、统一模型、机制图、官方事件时间线、固定版本实验、反例、结论、参考文献与补充推导。
- 初次91页合并输出全页渲染检查后，分离完整长表；两处结果表改为不可跨页表格；X/Y支路定义与真实图件统一为X留在donor、Y转移到helper。
- 数值曲线与时间线由已有冻结原件重绘，移除画布内的图题与说明；机制图保留必要局部标签，外部解释移入正文/图注。未把旧的手工矢量重绘稿放入正文。
- 最终页数、文件哈希、构建警告检查和图件数见检查点目录 `validation.json`。渲染检查包含逐页缩略总览与图件/表格页放大；它不等于独立科学审读。
- 超图透明PNG确认RGBA及实际alpha，白纸渲染无黑底；字节费用只作为Step2前代理，不冒称完整COPY或Makespan。
- Fang核对请求已发：issue26#issuecomment-5843634528；首版交付时未收到回应，不宣称已完成成员图件总验收。

## 最终提交前仍需完成

1. 独立科学审读：特别是强化界的条件、算法覆盖、证书版本匹配和三题指标解释。
2. Fang汇总清单到达后，核对最新数据身份与重复图件，以纸面质量择优替换；不为已经正确的同用途图重复制作。
3. 固定算法可运行复现附件的跨版本依赖打包和实际运行说明；当前审阅包仅含排版源、图、数据表与来源收据，不冒称算法程序提交包。
4. 结合通读反馈压缩留白、优化主次结构；真实封面信息按用户选择后补。

## 统计解释

P1/P2主曲线K1按题目定义为1，优化单核比值另报；P3保留实际同计划曲线。逐图比值算术平均与总量比不同。P3逻辑scheduled COPY不称为Cache后物理DDR流量。所有墙钟来自各自原批次，不把R9F时间记为Forest时间。


## 检查点02：全文重写与标注冻结

本节覆盖上文关于当前稿的状态：CP01原件保留，CP02正文31页、附表57页。新三张机制图实际为RGB白底，未收到透明alpha，不称为透明；仍按用户允许直接采用的两阶段PNG路线使用。数据图由冻结输入导出，新增子树排序图为明确构造示例。全部主文页面以1000像素长边渲染逐页查看，另核原图标签、表格和证明符号；没有新增独立硬件或科学验证。

两遍XeLaTeX日志无Overfull、未定义引用、缺字或警告。全部短表改为不跨页排版，两组方案时间线分别成对同页；相较旧版第7、12页的大面积留白已改善。空白检测只作提示，不代替整页阅读。

747个旧TX-ID全部保留并回交，376条源句无遗漏；新稿847个源单元和16个图文件标签另列。重构小节不是逐句一一等价，映射已注明粒度。33项术语记录有实际定义位置，未核到官方中文译名的项目明确标记。全部仍是作者提交状态，不自动变为独立通过。

用户要求软件内PDF标注后，CP02哈希与页码冻结；后续问题进入CP03。
