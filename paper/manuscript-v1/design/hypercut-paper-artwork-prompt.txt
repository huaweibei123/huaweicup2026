Edit target: the attached Chinese scientific hypergraph figure. Produce ONLY the graphical artwork for embedding into a LaTeX paper, with a genuinely transparent alpha background (PNG RGBA). Do not draw a checkerboard, white page, global gradient, photographic backdrop or external shadow.

Keep the two side-by-side scientific panels, their attractive pale blue/teal/amber local fills, exact object layout, smooth orange incidence curves, typography, math and internal labels. Preserve every scientific object. Keep the local colored Core-region fills opaque/light so the group semantics remain readable when the outer canvas is transparent.

DELETE ALL content below the two main outer panel boxes: remove both "(a) 触及两个 Core" and "(b) 消费者联合迁移", the horizontal footer rule, the bottom global COPY formula and explanatory sentence. There must be NO figure number, caption, subfigure title, overall title, paragraph, disclaimer or footer. LaTeX will supply every caption and figure number. Trim away the newly empty lower area; use a wide composition naturally enclosing only the artwork with modest transparent margin.

KEEP inside left panel: "Core 0", "Core 1"; operation nodes u, v₁, v₂; "张量 t"; λ_t = 2; w_t(λ_t − 1) = w_t. u is in Core 0, v₁ and v₂ are in Core 1.
KEEP inside right panel: "Core 0"; u, v₁, v₂; "张量 t"; λ_t = 1; w_t(λ_t − 1) = 0. All three operations are in Core 0.
KEEP center action arrow from left toward right with the short internal Chinese action label "联合迁移消费者" on two lines.
ALL SIX orange curves are UNDIRECTED hyperedge incidence lines, absolutely no arrowheads on them. Only the large horizontal transformation arrow has an arrowhead.
Retain Core in English exactly, all mathematical symbols correctly typeset, 张量 and the action label in Chinese. Do not invent labels or text. Preserve the existing pleasing balance of labels, box padding and curves. This is a precise cleanup and alpha-background edit, not a redesign.
