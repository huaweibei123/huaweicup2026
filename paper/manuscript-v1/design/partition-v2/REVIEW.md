# 机制图：两阶段制作记录

用户已认可首张生图的视觉风格，并授权按图选择两阶段或三阶段，不强制代码复刻。

## 来源与阶段

- 结构源：`../../data/figure-inputs/p1-026-k5.json.gz`；机器可读裁剪清单：`../../figures/partition-story.json`。均由冻结原始图、计划和官方结果生成，新增求解／评价0次。
- 结构稿：`../../figures/fig-p1-cuts.svg`，只承担拓扑核对，不作为最终美术风格。
- 风格样张：`style-approved-sample.png`。原始提示词：`prompt-initial.txt`。
- 修订成图：`../../figures/fig-p1-cuts-generated.png`。修订提示词：`prompt-correction.txt`。使用内置imagegen，不使用CLI/API fallback。

## 修订后逐项目视核对

| 项目 | 实际核对结果 |
|---|---|
| 每面节点 | 710、712、714、716、720、722、724、726，各一次 |
| 内部依赖 | 710→720、712→720、714→722、716→722、720→724、722→724、724→726，两面均正确 |
| 父计划 | 八个所示节点均属于sg0、核心0 |
| 细化计划 | X={710,712,720}属sg0/核0；Y={714,716,722}属sg6/核1；J={724,726}属sg5/核0 |
| 边界标签 | t721、t723均2 B，已补齐t721；t719均2 B，进入726 |
| 端口方向 | 上方输入向下进入节点；右方t719向左；下方输出从726向下，已修正 |
| 全图结果 | Task数5→7，44114→42014 cycle，减少4.76039%（图中4.76%），COPY增加163852 B |
| 颜色与箭头 | 内部依赖实线；所示内部跨子图边为橙色虚线；外部端口点线 |
| 可读性 | 深色模糊页脚已改为浅底深字；整图及标签目视检查无遮挡。最终PDF165mm宽的纸面检查仍单列 |

外部端口是裁剪表示：上方四个端口各合并该ADD的两个图外输入；t719的生产者718未绘制；下方为t727及其图外消费者。端口细节在JSON中保留。图中Task数、Makespan和字节差来自完整图，不能解释为八节点片段单独测得的成绩。

PNG为1536×1024；这是模型实际返回分辨率，不声称获得3072像素或矢量。165mm宽约236 ppi，最终排版以可读性实测判断；需要更高分辨率时再渲染或采用代码复刻，不以简单插值冒充新增细节。

保留科学准确性与视觉验收两个状态；生图完成、用户认可风格都不自动代表最终论文验收。
