# 论文工作区：从题意、证据到章节

当前采用**分章节 Markdown 迭代**。LaTeX 只在正文与关键图表形成可检查版本时接入；模板中的文字、数字与图均为示例，不是本队成果。

这次整理保留原目录和作者原稿，以入口、章节工作副本和固定来源消除查找困难。没有移动或删除历史资料。资料盘点截至 2026-09-26；本轮基础提交为 `e82c20098eb53cfb00d5f2287173f6894b3fb291`，未合并分支材料另记完整 SHA。

## 从这里读

| 要解决的问题 | 入口 |
|---|---|
| 全文怎样回应原题、怎样展开 | [章节框架与写作顺序](chapters/00-outline.md) |
| Coherent 怎样落到统一模型 | [第 3 章连接稿](chapters/03-unified-model.md) |
| 已有正文在哪里 | [第 4 章 P1](chapters/04-problem-1.md)、[第 5 章 P2](chapters/05-problem-2.md)、[第 6 章 P3](chapters/06-problem-3.md) |
| 哪些结论可写、哪些还只是研究 | [证据、理论与创新主张](planning/EVIDENCE_AND_THEORY.md) |
| 队友画了什么、脚本在哪、还缺什么 | [18 组图件盘点](planning/FIGURES.md) |
| Web Clip 专项任务从哪里开始 | [归档核验交接单](planning/WEB_CLIP_HANDOFF.md) |
| 参考材料与原稿来自哪个版本 | [来源目录](sources/README.md)、[机器可读清单](sources/manifest.json) |
| 本次工作交付和边界 | [任务卡](../tasks/paper/PAPER-PLANNING.md) |

**当前最需要处理的三件事**：P1 原稿仍基于旧 v4，要整章升级到已有 full500 的 `834d8c9`；P3 R9F已发布固定原件与汇总图，500份输出仍与Forest相同，保留 Forest 主算法；P3图6-3已补齐正确口径的研究主曲线，待统一版式/图注与正式验收；其他历史统计图仍不能直接代替同一固定算法的完整主图。P2 最新 C04 六格全部退化，保留 c665 主算法，将其作为有边界的机制反例。

## 目录与编辑规则

- `chapters/`：整稿工作副本。第 4～6 章继承已有作者草稿，页首明确旧版本、更新缺口与来源；不是三篇新验收论文。以此作为后续合稿入口，作者原稿仍保留。
- `planning/`：编辑决策、图件状态、证据边界和交接；不随正文提交赛事。
- `sources/`：固定提交、原件哈希、引用索引；不重复打包大型结果、PDF 或 Web Clip。
- `sections/`、`notes/`、`support/`：部分仍只存在于作者 PR。它们是本次工作副本的来源；合稿期间不自动双向同步。后续作者更新应指明源 SHA，由整稿者比较后吸收，不能静默覆盖。
- `template-2026/`：保留既有排版模板。下述命令仅供检查点使用，本次未运行编译。

每一处主成绩更新同步修改：算法说明、固定版本、表格、图与图注、计时边界、证据索引。单格改进、历史逐格最优拼接和未完成批次不得替换完整主表。章节中的 `[S…]` 是工作引用，定稿时分成正式文献、算法定义和附录复现信息；ChatGPT 回答本身不能替代证明或学术文献。

第一次 LaTeX 检查点建议设在：三问主版本与指标已冻结、所有章节至少形成一版连贯正文、必需主图已有可读导出、符号和交叉引用统一之后。第二次检查点处理匿名、页数、参考文献和附件格式。不要因一次小改动反复转换格式。

## 既有 LaTeX 模板说明

当前论文模板是 [`template-2026/`](template-2026/README.md)。它替换了此前用于训练的 2025 社区模板；旧版仅保留在 Git 历史中。模板中的姓名、队号、正文、图、数据和文献都是排版示例，不是本队的比赛成果。

在仓库根目录运行：

```sh
cd paper/template-2026
latexmk -outdir=../build main.tex       # 带封面：paper/build/main.pdf
latexmk -outdir=../build anonymous.tex  # 无封面：paper/build/anonymous.pdf
```

需要 XeLaTeX、latexmk、BibTeX 和支持中文的 TeX 环境。编译产物放在 Git 忽略的 `paper/build/`，不会覆盖模板自带的预览。正式参赛信息优先写入 Git 忽略的 `template-2026/config.local.tex`；正文编辑 `sections/`，参考文献编辑 `reference.bib`。把已验收的项目图件从根目录 `figures/` 引入模板 `figures/` 时保留来源与相对路径，论文中的数字须对应 `results/` 的实际产物。

压缩包原样解压，逐文件哈希见 [`template-source.json`](template-source.json)。模板代码的 MIT 许可不自动涵盖字体、Logo 和官方附件；分发或提交前应核对各自许可。模板自称参照 2026 年附件 2、3，但正式交稿仍须按当届赛事公告核对封面、匿名要求、页数和附件。本机编译成功仅证明排版链路可运行，不构成赛事格式验收。

## A 题绘图任务

[A 题绘图分工与验收任务书](notes/A-FIGURE-WORK-PLAN.md)列出甲独立负责的 10 组图、乙独立负责的 8 组图，以及逐图绘图流程、输入、交付指标和验收标准。图 5-4 全部由甲负责；文末提供三问初稿及证据索引的固定提交入口。
