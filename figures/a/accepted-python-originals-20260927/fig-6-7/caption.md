图6-7：P3 forest（算法311322b）revision2完整100用例×1–5核，共500格。上图为同一次运行的官方Makespan（cycles）与完整求解墙钟（s），双对数轴；下图按核数给每组100用例的经验累积分布，保留全部尾部。表内Median、P95（线性插值）和Max由原始逐格墙钟重算。最大分核P95为20.887373 s，不是全量500格P95；最大单次墙钟为51.335296 s。计时从子进程启动前至回收、清理和日志写入后，包含读图、构造、在线E0选优/核验、最终方案和证据写出；父进程预检另记batch wall，不含在图中单例墙钟。没有独立外部最终评价，evaluation_wall_seconds为null，不能当0；trace内evaluation_seconds是在线内部诊断，不替代外部墙钟。环境为Apple M5 Pro、48 GiB、macOS27 arm64、Python3.12.13；每分片worker=1、全局最多4个分片并发，线程数和峰值RSS未记录，不代表独占机器。每格单次观测，未控制OS缓存，不据此宣称严格冷启动、重复实验误差、跨平台速度或算法Pareto前沿。LYX原提交03f445ac图件保留，工作台仅修可复现源、审计格式及版式；未运行任何新求解或评价。
