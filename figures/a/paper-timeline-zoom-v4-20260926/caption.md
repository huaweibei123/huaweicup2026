# case_026 时间线候选图注（完整图与局部图分别编号，实际图号由合稿生成）

**完整时间线图**：用例 `case_026` 在 5 核配置下的两种方案，数据来自冻结的官方模拟事件。(a) 原方案的完成时间为 44,114 个模拟时钟周期，额外数据搬运量为 211,680 B；(b) 重新分配计算分支后，完成时间为 42,014 个模拟时钟周期，额外数据搬运量为 375,532 B。两方案共用 0–44,114 cycle 横轴。每个核心的 `Task` 行表示其任务执行区间；其余四行分别表示题面 `PIPE_M`、`PIPE_V`、`PIPE_MTE2`、`PIPE_MTE3` 四类硬件执行流水线（pipeline）的已记录操作区间，图内简写为 `M`、`V`、`MTE2`、`MTE3`。这里的 `M` 只指 `PIPE_M`，不表示方案完成时间。浅色区域标出局部放大图使用的 5,200–9,400 cycle 时间窗。

**局部放大图**：在相同的 5,200–9,400 cycle 时间窗内比较上述两方案。`C0:T0/SG0` 表示核心 0 上编号为 0 的任务及其对应的编号为 0 的子图；其余 `C`、`T`、`SG` 标识按此方式读取。局部图只显示核心 0 和核心 1 的原始事件，两方案各自的任务编号和操作区间均来自同一份冻结输入。此图使用独立图号，并在图注中引用前面的完整时间线图。

# case_019 时间线候选图注（完整图与局部图分别编号，实际图号由合稿生成）

**完整时间线图**：用例 `case_019` 在 5 核配置下的两种方案，数据来自冻结的官方模拟事件。(a) 本文主算法的完成时间为 16,247 个模拟时钟周期，额外数据搬运量为 495,120 B；(b) 前期研发中代码代号为 `C04` 的对照策略，完成时间为 28,514 个模拟时钟周期，额外数据搬运量为 132,692 B。两方案共用 0–28,514 cycle 横轴。`Task` 表示核心上的任务执行区间，`M`、`V`、`MTE2`、`MTE3` 分别表示题面四类硬件执行流水线（pipeline）的已记录操作区间。浅色区域标出局部放大图使用的 13,800–17,400 cycle 时间窗。

**局部放大图**：在相同的 13,800–17,400 cycle 时间窗内比较上述两方案。主算法中核心 3 上编号为 3 的任务在 16,247 cycle 完成；对照策略中核心 0 上编号为 0 的任务在这一时刻仍在执行。图内 `C0:T0` 表示核心 0 上编号为 0 的任务。问题二的一项任务可能包含多个子图，因而不能把 `T0` 解释为单个子图编号。此图使用独立图号，并在图注中引用前面的完整时间线图。

两例中，执行资源对应行的空白仅表示该时段没有记录的占用事件，不能据此判定其原因是等待外部内存、片上存储或其他资源。
