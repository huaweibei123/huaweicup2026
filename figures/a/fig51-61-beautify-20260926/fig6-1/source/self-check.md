# 图 6-1 自查记录（甲，v2，2026-09-26）

## 实际执行结果（本机制图环境）

- 导出：draw.io CLI（--disable-gpu --no-sandbox）rc=0，SVG viewBox 711×830、PNG 1422×1660（scale=2）。
- validate.py：0 error / 20 warnings（容器与样式提示）；0 处边交叉。
- 尺寸与字号（fontSize×W/711×72/25.4）：最小 10px → 6.58pt@165mm；主标签 10.5–13px → 6.9–8.55pt。165mm 宽插入高 192.6mm，正文页内容纳。

## F61 逐条自查（对应 5844880637）

1. R01（科学公式，必须修）：C=M₃/M₂ 已全包更正为 **C=M₂/M₃**（式 6-4 原文核对一致；M₂=同计划无 L2，M₃=只读 Cache）；图注显式给分子分母；C>1 改善、C<1 退化；候选接受仍只比较 M₃。解释性算例：M₂=120、M₃=100 → C=1.2（改善；仅验证公式，不填论文实测）。全包搜索无倒数定义残留。已完成。
2. R02（流程条件与模块归属，必须修）：基础结构路由先构造并评价基准（取得当前合法方案/已用 calls），增量候选②③④⑤按守卫与剩余预算在其后尝试；同一组去重/条件剪枝/E0/严格接受节点复用；实际调用链 expanded→calendar→pipeline→witness→forest 在大框标注；①基础路线仅守卫拒绝才下一路；⑤forest 不再写成 witness 内部。prune 补 :78–112 适用检查五条；基准评分先于比较；预算耗尽不再评分；剪枝/重复跳过评分而继续剩余流程。按源码分支逐条走通。已完成。
3. R03（来源登记）：8 项 sources 逐文件纯路径+完整 commit 311322b996c0948e8a6a9c7ec6ddfe6ae41fbee1+Git blob 字节 SHA256（与审阅方本机哈希逐项一致）；476/24 注明初稿 6.6.1 已发布批次口径。已完成。
4. R04（表结构四格）：conditional_prune / official_p3 / strict_improvement / same_plan_no_l2 已改；structure/candidate/deduplicate 满足；节点 12、边 11 与 drawio 一致，无缺端点/重复边/空 module。已完成。
5. R05（实际视觉）：idx 两行加高不贴边；ext 短行无孤字、标题改短无孤行；e6 标签移除、条件并入 e0 首行，无压框；未用缩字方式。已完成。

## 验收标准维持

- 主流程对应当前完整算法（forest_solve@311322b，无旧入口符号）。
- E0 节点显式：求解器不可修改官方 Cache 替换规则、容量与带宽。
- 同计划无 L2 对照独立虚线分支；与独立优化的 P2 主算法比较不混同。

## 历史稿

v1=7db0a1cac（首版，C=M₃/M₂ 公式反写）；v2=本版。均保留于 git 历史。

未完成项：无。
