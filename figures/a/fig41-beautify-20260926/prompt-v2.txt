Edit the supplied generated diagram. Keep ALL existing text, colors, boxes and upper workflow exactly unchanged. Fix ONLY lower workflow connectors and transparent edge quality. This is a scientific diagram: exact graph connectivity overrides aesthetic symmetry.

1. The orange arrow from bottom of “官方方案文件（严格仅两字段）” currently points at the lower panel title/border near the center. DELETE that center downward arrow. REPLACE it by an orthogonal orange arrow starting at the LEFT region of the plan interface lower edge, passing through the gap BETWEEN panels, then down to the TOP CENTER of the lower LEFTMOST “读入方案文件” card. Route around the orange panel title; do not touch or cross the title or any text. Allow slightly more vertical gap between the two panels ONLY if necessary for clean routing.

2. The orange metric-output line currently starts at the bottom of “官方 Core 内调度”, which falsely implies direct metric output. DELETE the vertical segment from that scheduling box to the metric bus. The ONLY source of BOTH bottom output arrows must be bottom-center of “多 Core 事件模拟”. From that source, draw an orthogonal branch through the empty gap above output boxes to BOTH “Makespan (cycles)” and “额外数据搬运量（B）”, each with a distinct down arrowhead at the top of its output card. Preserve scheduling -> simulation horizontal arrow.

3. Keep E0 as a dashed explanatory annotation associated with the official evaluation layer, not a data output of the read step. Replace the read->E0 downward directed dashed arrow with a short NON-DIRECTIONAL dashed association, no arrowhead. No other changes to E0 text.

4. Clean accidental white jagged/glowing pixels outside outer panel outlines. Maintain TRUE alpha-transparent background, soft group fills, clean antialiased edges; no checkerboard or opaque background. No new title, footer or caption.

Preserve all six candidate labels, both E1/single-candidate branches, both JSON fields, every Chinese glyph, English token, number and unit. Do not rewrite or simplify any labels. The result must retain exact content with just the specified arrow corrections. High resolution PNG. No extra arrows, no feedback loops, no lines through boxes/text.
