Precise cleanup of supplied figure. Preserve its current Song/Ming Chinese typography, colors, content and layout.
1. REMOVE the entire blue/beige blurred background OUTSIDE the two rounded outer panels. The output MUST be a transparent-background PNG with actual alpha=0 outside panels. No opaque background, gradient or checkerboard. Keep panel fills opaque.
2. DELETE the short central orange downward arrow between the panels (at roughly x=735 in the 1536px image). Instead draw an orange straight vertical arrow at x=220, starting at the BOTTOM BORDER of the upper submission-file band (y~700) and ending at the TOP BORDER of the lower left “读取提交文件” card (y~790). Its path is through blank whitespace only. The lower panel heading is centered at x~770 so x=220 does not cross it. Do not connect this arrow to the single-candidate card or start it above submission-file band.
3. Correct and clean the first purple candidate card's text to exactly two readable lines: “① 按连通分量与树结构划分 Task” then “（必需的初始方案）”. Ensure 树结构 is legible, not garbled. Correct fourth card line “选择参与计算的 Core” (not 计算故读). All other text unchanged.
Return the corrected academic diagram, SAME dimensions or higher native resolution, true transparent PNG. No new labels, no other layout changes.
