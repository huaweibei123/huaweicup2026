# 图 5-3 修改自查

- **F53-R01 方法与覆盖**：`tensor`、`gap`、`c665` 各 500 个唯一的计算图/核心数坐标，每核 100 张；`F1` 仅五核心 100 张。右侧五核心数值来自左侧同一批结果。未加入缺测值，也未拼接不同版本逐格最优。
- **F53-R02 共同分母**：对每张计算图，三个成员表的官方情况 A 单核心分母及基线收据一致；`c665` 使用该共同分母而非前一优化版本的 `row.baseline`。用例 002 分母为 261945 周期。三种方法的一核心真实平均值都画在曲线上，分别为 1.056973、1.167562、1.206139；灰色理想线的一核心值 1 仅为参照。
- **F53-R03 规范数据表**：长表 1,600 行，具有 `case,cores,method,baseline,makespan,speedup`；`F1` 字段精确为 `F1`。汇总表 16 行，具有 `method,cores,n,mean_speedup,mean_observed_speedup,display_policy`，没有用 0 补齐缺测。
- **F53-R04 单图重现**：实际执行 `uv run --locked python src/analysis/fig53_rework.py --figure 5-3`，只读固定交接 ZIP；源码、输入和命令都在当前提交，不依赖未提交的本机输入，没有运行整套绘图或任何求解/评价程序。
- **F53-R05 最终字节**：`manifest.json` 列出最终图、表、图注、说明和检查文件的实际 SHA-256；`audit.json` 再核对清单、源码与交接 ZIP，自身不作循环哈希。发布后还须按远端 Git 对象逐项回读，不能仅凭本机检查宣布远端通过。

视觉检查：2026-09-27 在 macOS 上实际查看原分辨率 PNG 和按 96 dpi 渲染的 160 mm PDF 预览。PDF 为单页，页面尺寸 453.543 × 249.449 pt（160 × 88 mm）；中文、图例、线型、两个接近的五核心数值及图边界可辨，未见裁切或重叠。`pdftotext` 可以读回中文。论文监督会话是否选版及插入后整页效果另行核查；本自查不代替最终验收。
