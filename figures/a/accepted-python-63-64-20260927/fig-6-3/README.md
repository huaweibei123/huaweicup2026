# 6-3 完整配对候选

原LYX 96e819b7旧witness图保留；本机按用户授权更新forest311322b revision2，复用farmer babdd74c的500对输入。与6-4的pairs.csv字节相同。原件验证见source-verification.json及raw-pair-verification.json，全部1000压缩结果、1000计划引用实读校验，内容寻址相同的文件允许缓存复用；没有新solver/E0。

在本目录执行 `python plot_local.py --prepared .` 从随包summary.csv生成图；绘图代码为工作台自编。生成规范表的本机只读代码为prepare_cache_tables.py，输入参数说明见源码；输出匹配原配对结果。统一基线固定路径、SHA及原件入口见sources.json。

已实际查看160mm宽PDF预览，两种线型/点形、数值表和图注明确。当前为候选，仍待Agent记录与用户全套总验收；未生成论文finals。
