Edit this figure minimally. Keep EVERY node, every directed edge, every Task/Core label, all task assignments, arrow directions, node positions and bottom diagram unchanged. The only scientific-clarity correction: in the RIGHT upper DAG, the dashed enclosing stage0 and stage1 rectangles overlap the node a. REMOVE BOTH large outer dashed rectangles and their background fills completely. Replace them by neat Draw.io-style vertical BRACKETS to the left of the right DAG: a short gray square bracket spanning s through a labelled 阶段 0; a second separate gray square bracket spanning b1/b2/b3 through t labelled 阶段 1. Distinct non-overlapping stage ranges, no enclosing stage rectangle. Keep the three teal chain enclosures and amber r/t enclosure unchanged. Leave all 13 DAG edges unchanged. Preserve each graph 12 nodes. Keep gray neutral s,a; teal chain groups; amber r,t. Remove the two optional sentence annotations next to r,t to provide whitespace. Keep the bottom legend. Overall maintain same captain-style pastel academic Draw.io aesthetic and exact diagram, sharp serif text. Background must be native transparent alpha or plain white, no visible shadow/texture/backdrop. No new title/caption.
