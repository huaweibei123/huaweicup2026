图 X　官方 100 张原始计算图的规模与计算操作时长构成。（a）每点是一张计算图；横轴是非 COPY 计算操作的数量，纵轴是图中所有张量的大小总和。两轴均采用对数刻度，张量大小的单位为 MiB。（b）对每张图，分别汇总矩阵类和向量类计算操作在题面模型中的标称时长，以模拟时钟周期计；纵坐标是矩阵类操作时长之和除以这两类操作时长之和。横轴按该比值从小到大排列 100 张图，每张图保留一个位置。这个无量纲占比描述静态计算工作量构成，不能解释为调度后的执行时间占比或总完成时间（Makespan）。

图内和图注均不再用单独的 M 同时表示矩阵工作量与总完成时间。图中数值与冻结 v7 数据逐项相同；正式图号由 LaTeX 确定。
