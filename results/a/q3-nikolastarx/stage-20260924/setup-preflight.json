{
  "stage": "before first successful runner dispatch",
  "E0_calls": 0,
  "solver_processes": 0,
  "failures": [
    "fresh detached worktree lacked ignored official case JSON",
    "temporary read-only symlink escaped runner resolved repository-path contract"
  ],
  "resolution": "Copied the three original JSON files byte-for-byte into ignored raw locations, then fixed source-manifest SHA checks passed. No original raw file modified. Both failures before batch directory/child creation; no scorer retries.",
  "dependency_setup": "uv sync --locked; CPython3.12.13; cached13 packages; setup is offline preparation, not solver wall."
}
