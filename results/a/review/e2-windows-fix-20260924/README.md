# Windows P1 平台补丁定点回归

原测试PR44 / `6aad0ef09521913de49fb91c5624fd8194e1728e`及原P1 `f4ee475`保持不变。本分支只应用队长`51f462d56f86f11d1fc7d6bbb25f738ee8297626`相对父提交的四文件补丁：WINDOWS_FIX.md、_resources.py、pool.py、tests/test_resources.py。应用前git apply --check通过；补丁固定`43bfd6769d77fdb0e41372ab46bbeef997947848`，含测试控制器的实际准备HEAD为`d5e0e172476863e29e5d542fe3e8d473a4b251aa`。没有引入P2/P3源码或作者macOS收据，没有自行修核心。

## 窗口2：明确重新授权后，定点回归通过

本机协调在读取窗口1零执行证据后明确批准新600秒窗口。新T0为`2026-09-23T22:51:25.0724896Z`，截止`23:01:25.0724896Z`；窗口1原记录未覆盖。实跑HEAD为`38c8c0e3744f47f9c4a48510c601c56b1598034a`，与d5e0e172的驱动/被测核心相同，仅多旧窗口记录。未修改驱动、重新编译或安装。

3项ResourceTest、2项原P1 pool测试 **5/5通过**，另1个结构化probe通过。测试于22:52:31.360448Z结束，自T0含准备/补读/启动/测试/退出共66.287959秒；交付整理继续计入同一窗口，最终保全时刻见`PUBLICATION.json`。没有重跑64池或488全套。

- RSS probe两个worker峰值分别35495936/35655680字节，PID31276→2368，均因`peak_rss_threshold`回收；缺遥测控制注入两条也按`rss_telemetry_unavailable`回收。不是硬RSS限额或峰值采样全覆盖证明。
- Probe的8个极短1e-12秒请求全部timeout；ResourceTest另8个极短请求、旧pool测试另1个timeout也按预期。正常30秒预算请求走native，Makespan104；显式模拟缺库走e1_fallback，合成E0真值104一致。缺库是`get_lib`故障注入，不宣称本轮再次搬走物理DLL。时钟信息保留，pool请求deadline采用perf_counter。
- 每个阶段都由外层kill-on-close Windows Job覆盖父子进程。四阶段returncode=0、active_before_cleanup=0、active_after_cleanup=0，没有外层强制回收；测试内部主动超时/杀worker属于预定测试。06:52:58进程快照无Python。没有把池timeout、缓存额度或遥测值当整个作业/硬内存上限。

### 耐久预留与实际计数

|阶段|预留请求/可能真值占用/显式E0|实际pool输入/返回记录|直接E2/E0|
|---|---|---|---|
|resources（3测试）|12/12/0|12/12（含2条无评价控制注入）|0/0|
|pool_order|8/8/0|8/8|0/0|
|pool_cancel|6/6/0|6/5，取消后1条未向调用者交付|0/0|
|probe|12/13/1|11/11|1/1|
|总计|38/39/1|37/36|1/1|

扣除2条控制注入，实际合成E2请求/尝试36，直接合成E0 **1次成功**，正式图/正式候选/E0 **0**。父端已返回记录包括15 native成功、17 timeout、1 worker_error、1 invalid/e1_fallback、2控制记录；probe另有1条直接E2的故障注入fallback。17个timeout和1条未交付记录的内部执行进度保持未知，不退预留、不把未知当非法；观察到的E1 fallback为2条（非法1、缺库注入1），不是另488次。全窗口潜在真值/回退占用保守保留39，不冒称未知都没调用。阶段账本均在启动前fsync落盘。

`window2/`含未裁剪的阶段stdout/stderr、child和Job控制收据、时钟、DLL/代码身份及账本；公开路径脱敏，原件从进程启动起保存在所有Git工作区之外，原始/共享SHA对应表见`WINDOW2_MANIFEST.json`。曾在只读汇总日志时遇到默认GBK解码UTF-8报错，随后显式UTF-8读取同一文件成功；没有为该后处理问题重新运行任何测试或评价。

结论只关闭本机这两个P1平台反例的定点复现；不能升级为全Windows输入域、完整E2发布或P2/P3独立验收。没有改Atlas或自行终验。官方目标逐会话四点理解见[OBJECTIVES_RECEIPT.md](OBJECTIVES_RECEIPT.md)。

## 窗口1：期限已过，未启动任何测试

T0 `2026-09-23T22:12:24.3862632Z`，600秒固定截止`22:22:24.3862632Z`。准备/续接期间墙钟已经越过截止；实际调用控制器时为`22:47:59Z`。它在任何阶段预留/创建进程之前返回`global_deadline_no_stage_started`，退出码1，`stages=[]`。不能把超过期限的2134.679秒整体说成在600秒内完成，也不能报告修复测试通过。

- 实际合成E2=0，显式/潜在真值预留=0、直接E0=0，正式候选/E0=0。
- 未启动Windows Job或任何测试worker；22:48:11进程快照未见Python。
- 环境和已存在三份DLL的哈希已复核，沿用PR44工具链；这不是本轮native执行证明。
- 原始T0、账本、环境记录在Git工作区外保留；`window1/`提供原样副本，均不含个人路径。旧测试证据、当前源代码及预算均未重置。
- 已向本机协调报告，需要新的明确窗口才可执行；原批准窗口不自动延长。未运行的测试保持未验证。

## 已冻结的运行设计

入口`research/a/review/e2_windows_20260924/fix_regression.py --private <工作区外的新窗口目录>`；私有目录先保存授权对应的T0.txt。控制器读取它形成600秒期限，逐阶段先fsync耐久预留账本，再创建kill-on-close Windows Job并把等待gate的子进程加入Job，包含其后代；阶段90秒且受总剩余时间限制，预留退出清理时间，首次失败收束。准备开销也记入同一T0。该控制器在窗口1未走到实际Job创建，不能把设计写成已经验证的进程回收。

批准的最大小范围为3项ResourceTest、2项原P1 pool测试及一个可选probe：阶段预留12/8/6/12合成请求（其中2条控制注入不评价），总38≤48；潜在真值/回退/未知占用39≤64，显式合成E0最多1≤4。旧功能测试短时2worker，其他1worker；0正式图、0正式候选，不运行488全套或64池。计数包装器关闭底层迭代器，不修改被测核心。原始stdout/stderr从进程启动即落在Git外私有目录，发布时脱敏和记录双hash。
