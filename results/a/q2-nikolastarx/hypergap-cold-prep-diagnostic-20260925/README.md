# 已有评分记录中的冷准备开销

只读提取新c665批次的003/k5、014/k1、014/k2原生E2已有记录，新增评分调用为0；每份ledger身份见timing-extract.json。所选计划已有独立E0，但该E0耗时不能直接充当另外两个计划的对照耗时，也不是独占硬件基准。

014/k2三个不同完整计划的prepare约5.420/7.093/7.156秒，native replay约1.3/2.3/2.5毫秒，完整solver31.094秒。这支持优先审查冷准备和适配开销，不能推出改写某一个阶段就能获得对应加速。完整plan缓存各有一次miss；候选不同，不声称简单共享实例就能命中。已向E2负责人提出只读接口/失效边界审查，不修改在途冻结源码、不新增profile或benchmark。完整原件随后随新批次的合法成绩feed按原身份归档，当前是带原件哈希的研究提取。

## 基础设施负责人的只读审查

来源：E2 基础设施 session `nikolastarx/s-55b66a31d7bd49019122a179563dc1d2`，2026-09-25 本轮会话回复；属于源码审查意见，未完成新 profile 或提速实验。

完整计划缓存键包含 plan 与 bandwidth/capacity/cache-bandwidth，不同候选各 miss 一次符合当前接口。可以优先复用绑定图字节和官方版本的不可变图索引，例如 op/tensor ID 表及原始 producer/consumer/direct-edge 视图。每个不同计划的分核派生、跨核 COPY 构造、Step1 排序、Step2 spill、Step3 准备、执行验证与 native 打包仍需分别处理。

仅 op→core 相同不足以证明每核编译缓存可复用：子图及核内顺序、COPY 归属/优先级、其他核引起的全局 COPY ID 变化都可能影响后续结果。更深的缓存需要证明完整 lowered graph（ID、边、顺序、配置）等价或给出安全重映射。当前没有逐阶段耗时证据，不能推断只缓存图索引就有显著收益；在途全量版本继续冻结。
