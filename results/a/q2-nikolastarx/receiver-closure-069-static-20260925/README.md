# 069/K5：完整执行约束审计与接收闭包交换

**这是未评分机制试验，不是新 benchmark。** 统一 full500 的既有 5 核均值仍为 4.5497569967×。新候选的 Makespan 尚未知，不能用静态下界或减少的字节代替官方成绩。

- 构造源码 `9f89c2b4a4d0b7acd37da8c88f1b860c364e9c66`；云端封包 runner `0c452f09002f8ce0c635308d60d7eaf37a1cccf2`。`manifest.json` 与 `freeze.json` 保存输入、源码、ZIP 原始字节哈希；云端逐文件核验，不宣称云端 Git checkout 验证。
- 原图、配置与种子取自 c665 统一算法已验的 069/K5。旧官方 Makespan 11962；没有重跑种子的最终评分。新候选原始 SHA-256 为 `8ace171ac9c837a8986ee8baf950be96a694baabd99ca622023dfa268274b496`。
- 本机原方案在内存预检阶段停止，attempt/prepare/construct 均为 0。另获独立 CPU Standard 窗口后运行一次：主机 T0 2026-09-25 06:58:28.927893Z，云端被测子进程 06:58:52–53Z，Python 3.13.15。
- 官方 Step1/2/3 prepare 1 次（含局部模拟，0.179392 秒），RCX 构造 1 次（1.043739 秒）；最终 E0/E1/E2 均 0，无重试。云端活动 1.606556 秒；采样子树峰值 37,826,560 B，含观察者峰值 59,367,424 B；退出 0，无幸存子进程。
- 全部操作发射门槛与已保存 trace 一致，无残差。7 条跨核连接属于回溯得到的紧路径，176 个操作在该紧子图内。暴露延迟是原运行的条件解释，不能累加成候选节省。
- 本次检查 7 个候选，1 个核内顺序联合图成环而拒绝，6 个通过静态守卫。最终把 tensor 1000000494 在核 1 的消费者 op460 移至生产者核 2，未用补偿包；必需 COPY 字节模型 447992→447928，固定 FIFO 计算下界 8311。以上均不是新候选的官方 Makespan 或实测 DDR。

`results.zip` 为实际取得的云端原件（266099 B、13 个唯一成员、CRC 通过），含新方案、prepared pickle、完整约束 JSON、审计、进程及调用收据。根目录 JSON 是便于读取的原件副本。root 只读取约束 JSON 与旧 E0 trace 重审，完整审计 JSON 与云端一致；未加载 pickle 执行，也未再调用准备或构造。

CPU VM stop 返回 0，随后两次新鲜 sessions 查询均为空，独立 watchdog 已回收。`controller.py`、`bootstrap.py`、`test_controller.py` 是原运行的胶水原件；它们依赖原封包布局，不能在本归档目录直接启动。假 CLI 测试 5 项通过，只证明测试范围内的空会话识别、源码漂移拦截、租约与父进程退出后释放；不替代真实运行回执。

下一步仅对固定候选做独立官方评分。若有收益，再验证是否能形成通用构造并完成同一算法的全量成绩；本记录不能用于逐格择优拼接。
