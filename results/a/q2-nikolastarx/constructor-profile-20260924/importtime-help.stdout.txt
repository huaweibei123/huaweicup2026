usage: direct_solve.py [-h] [--config CONFIG] --cores CORES --output OUTPUT
                       --evidence EVIDENCE [--wall WALL]
                       [--evaluation-timeout EVALUATION_TIMEOUT]
                       graph

Single-pass P2 router: guarded resource word, otherwise tensor-aware DAG. No
evaluator or per-case result is consulted. Structural validation is not an
official performance certificate; the benchmark launches final E0 separately.

positional arguments:
  graph

options:
  -h, --help            show this help message and exit
  --config CONFIG
  --cores CORES
  --output OUTPUT
  --evidence EVIDENCE
  --wall WALL
  --evaluation-timeout EVALUATION_TIMEOUT
                        Unused compatibility argument: this solver never
                        scores
