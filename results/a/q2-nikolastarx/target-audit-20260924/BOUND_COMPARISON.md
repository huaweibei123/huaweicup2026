# P2 全局下界与既有成绩的逐格交叉核对

结论：**给定这批全局下界论证成立，用户的 2/3/4/5 核目标 2.26/3.18/3.96/4.53 均未被数学界排除，严格超越这些目标也未被排除。** 这不证明目标可达；现有 500 格历史赢家都没有达到对应下界，不能据此宣布任何一格全局最优，更不能作为停止研发的证明。

本补充只做既有 JSON/源码身份/图身份与算术检查，E0/E1/E2 均为 0。成绩仍固定在原审计 **2026-09-24 14:45:49.529895 UTC** 快照，不导入后续成绩，不改变旧报告。

## 输入、身份与实际检查

静态证书原始输入按字节保存在 `BOUND_INPUT.json`，来源为主会话交给审计的临时全100扫描结果：

- 输入 SHA-256：`fe25f7b7737dbd3d841284bc5939242982613f64e0cffa9bd3d25df1f4a0c96b`。
- 证书生成源码 `src/q2_nikolastarx/global_bounds.py` SHA-256：`5862e1a2a6a884f3162d445219b7b2a7b1b5b8d162b374a78f12fd45a420c0c3`，与本次读取的工作区源码一致。
- 已读证明文档 `docs/a/q2-nikolastarx/OPTIMALITY_BOUNDS.md` SHA-256：`1532110e5fdc7c7b6f3bed3896b80b56b6516596acde341f6c25a4f700d99560`。
- 100 图原始字节全部与证书及官方单核分母引用的 graph SHA 一致；六份冻结官方源码 SHA 全部匹配。
- 100 图均 `supported=true`、`precedence_supported=true`，覆盖 1–5 核，500 个下界均为正整数。没有多 eligible producer 的 tensor，COPY-only 收缩增边计数均为0。
- 原审计的109处分母引用重新核对 gzip 字节 SHA、scene A 和 Makespan，100图同名分母一致。计算使用统一的这些官方单核分母；没有用优化后的P2单核方案替代。

逐格对照包括：历史官方已准入赢家 500 格、Fang 固定连续分块生产批次 500 格。每一项检查 problem/case/cores 及匹配图、配置、官方源码身份，然后比较整数 `U >= L`。共 **1000 次比较，0 个 `U<L` 异常**。因此没有需要归因的异常清单；完整空清单和1000次差值记录在 `BOUND_COMPARISON.json`。无异常只表示这些已知可行证据未反驳下界，不代替对所有合法方案的证明。

## 平均加速比上界

若每图全局最优 Makespan 为 `OPT_i,k`，证书为 `0 < L_i,k <= OPT_i,k`，官方单核基线为 `B_i > 0`，则任何合法求解结果 `C_i,k` 满足：

`mean_i(B_i / C_i,k) <= mean_i(B_i / OPT_i,k) <= mean_i(B_i / L_i,k)`。

按全部100图逐图相除再取算术平均；没有使用总周期之比。脚本用 `fractions.Fraction` 精确计算平均上界和目标比较，JSON保存精确分子/分母；表格只作十位小数显示。

| 核数 | Fang 固定基线均值 | 历史赢家均值 | 证书导出的均值上界 | 用户数值目标 | 上界减目标 | 已排除目标？ |
|---|---:|---:|---:|---:|---:|---|
| 1 | 1.0000000000 | 1.0000000000 | 1.2424989201 | 未给 | — | 不适用 |
| 2 | 1.4598378509 | 1.4598378509 | 2.4805789918 | 2.26 | 0.2205789918 | 否 |
| 3 | 1.8362181698 | 1.8362181698 | 3.7036829212 | 3.18 | 0.5236829212 | 否 |
| 4 | 2.0998070125 | 2.2056399787 | 4.9225591976 | 3.96 | 0.9625591976 | 否 |
| 5 | 2.2814611427 | 2.2814611427 | 6.1080415372 | 4.53 | 1.5780415372 | 否 |

单核一行的1.2425是“允许在P2内改变方案后的潜在加速比”的宽松上界。报告曲线把固定单核基线自身记为1，与此不矛盾。单核基线是启发式方案而非最优调度，因此多核上界超过核数也不自动构成错误。

图片本身缺少指标定义、图集与分母身份，本表只回答：**采用我们明确的官方100图平均口径，这些数值目标是否已被当前下界否定？没有。** 不能反过来认证图片来源或宣布图中算法可复现。

## 距离“不能再改善”的证据仍有多远

500 个历史赢家中 `U=L` 的数量是 **0**。按核数最小 `U-L` 分别为70、210、248、552、598 cycles；这些是各核数某一图的最小松弛，不是所有图可实现的改善量。`U/L-1` 仅是已获方案相对未知最优解的可证误差上限，逐格保存；不能解释为一定存在同等幅度改进。

下界含非COPY操作工作量、不可分割性、保留计算依赖的路径及head/tail窗口。不包含精确DDR服务时间、容量和额外搬运对时间的完整约束，故可能明显偏松。DDR字节界和求解wall界不是本表的性能上界，不能从本表推出“各指标同时最优”。

路径/窗口部分依赖冻结P2与Step2对原始计算依赖的保持论证，尤其跨核初次COPY_IN与后续reload在固定MTE2投影中的顺序。已读源码推导，另有子Agent做概念范围检查，未发现明显的work/packing/window不等式问题；这仍不等于完整独立证明验收。未来若出现 `U<L`，应先冻结该方案和证书，核查领域/图身份/证明与实现，不能通过丢弃官方可行结果来维护下界。

## 复算

在仓库根运行：

```sh
python3 results/a/q2-nikolastarx/target-audit-20260924/bound_comparison.py
```

脚本只读本目录冻结响应、`BOUND_INPUT.json`、已有分母原件、冻结图/官方源码及当前证书源码/证明文件的身份。输出 `BOUND_COMPARISON.json`；不访问网络，不导入求解器或evaluator，不修改旧快照。发生任何 `U<L` 会先保留完整异常条目，再以非零退出码结束。本次运行退出0，500格/1000次比较全部完成。
