# 原生重放接入：三张正式图的有限对照

2026-09-24，本机 macOS arm64 / CPython 3.12.13 / 锁定 NumPy 2.5.3 / Apple Clang 21。源码与运行说明在 `research/a/native-replay-20260924/`。研究输入来自 Pro r01 归档提交 `564114aafe60ef852ee9bb3f011c7856194edb54`，E1 基线固定为 `5bfe53a29c1ba05167239f51ea937e602f7f85b4`，E0 冻结源码 hash 见 `run.json`。

正式图 002/003/008，每图固定划分的 3 个不同核分配计划：9 次完整 E0/E1 结果类型和值零差分；原生 serial、coalesced 和 audit 模式的全部操作/Task 起止时刻零差分，audit 的全部 DDR 发射投影记录也一致。三个模式不是 27 个不同计划；后续三轮计时重复同 3 个计划，也不计为新候选。

| 图 | 编译后操作数 / Task 数 | 热准备对象评分相对 E1 缓存调用 | 计入首次准备的 3 候选批次比值 |
| --- | --- | --- | --- |
| 002 | 5,577 / 26 | 206.6× | 0.426× |
| 003 | 34,359 / 182 | 277.8× | 1.926× |
| 008 | 2,608 / 11 | 173.0× | 1.951× |

热评分使用预先编译的内部接口，返回时刻数组而非官方完整 JSON；E1 当前缓存 API 仍逐候选执行完整输入检查、还原 Task 并生成所有结果，因此这不是等输出形态的语言性能比较。速度归因于准备对象复用、减少 Python 对象/结果构造与原生事件循环的组合，未完成各项独立消融。冷批比值为记录成本的加总：E1 首批与初始化 /（新实例、官方局部编译、数组打包、首次加载预热、该批热评分）。只计解析后的输入到结果，不含外部进程/文件 IO。

图 002 首次动态库加载和一次预热墙钟 0.7035 秒、CPU 0.00233 秒；原样收费，未删除这个不利观察。后两图处于同一进程，不再次加载动态库，不能把它们当作三个独立冷进程样本。本机存在其他桌面/Agent 活动，没有 CPU 亲和性隔离。

第一次运行发现 Pro 适配层 uint8 累加溢出，见相邻 `native-replay-probe-20260924/FAILURE.json`；失败记录保留。修复后本目录完整跑通。`as-run-source.zip` 保存本目录及后续 64 候选实验实际运行的四份原型源码；发布前只更正 `probe.py` 的说明性 docstring，算法未改。清单中的源码哈希可以与此 ZIP 成员逐项回读，E1 文件则在上述固定基线提交回读。

本机原生时刻精确证据限于这些输入。不证明任意整数、浮点等待、容量、异常路径、多平台或长期并发正确性，不是独立验收。
