# P2 full100 A 批测量与原件审计

本批按固定协议唯一运行一次，30 个用例均成功。只运行四核 `tensor_packet`；B/C 不包含在本批。这里的均值是部分样本，不能作为全部 100 图均值，也不能说明已超过中央当前最好方案。

- Spec：`e6deb8a78b45bda186577b853390fe7632354c2b`；solver：`e64723bdf99669c44f76d8e90ab0379a8578522e`；runner：`fa6522a3266fe040379dd064092beb27c0b20a5e`；E0：`45f647b395b84e9569f418fd33d62c2b8eb4d190`。
- 实际 T0：`2026-09-24T16:22:26.783963Z`；T1：`2026-09-24T16:25:57.614282Z`；批次墙钟 210.830734 秒。批前固定字节检查另计 6.182735 秒。
- 账本实际 30 solver、30 E0、0 E1、0 E2；单 worker，零重试。60 个阶段起止区间无重叠。
- solver 总墙钟 44.476737 秒，中位数 1.134208 秒，最大 4.187737 秒；独立 E0 总墙钟 145.863699 秒。
- A 的 30 图逐图单核/四核比值算术均值为 **3.381042549**。复用同一 solver 提交的 R4 十图后，40 个互不重复用例均值为 **3.341437821**。仍缺 60 图，完整均值未确定。

每次 solver 是新 Python 进程，墙钟覆盖启动、读取输入、在线结构处理、方案构造与落盘、退出和父进程持久记账开销。OS 文件缓存、普通系统及交互应用负载未控制；没有测量峰值 RSS。外部 E0 与压缩/报告处理不属于 solver 墙钟。本批开始前 P3 静态扫描已于 `2026-09-24T16:12:02.047734Z` 完成；按父任务协调消息，P1/P3 没有并行评分。该协调信息不是对整台机器的进程采样证明。

## 反馈与边界

相对于先前完整 100 图的旧连续分配固定来源 `6664a63adc3464d28d1f835d907cdeaea23e6b35`，本批 23 图改善、3 图持平、4 图退步。对照的图、配置、官方源码身份逐一一致；此旧来源不能代替当前中央最佳。

| 用例 | 当前 cycles | 旧连续 cycles | 当前 spill bytes | 当前路由 |
|---|---:|---:|---:|---|
| 001 | 78028 | 58984 | 622592 | shared_stages |
| 007 | 417689 | 259822 | 11727360 | shared_cohorts |
| 023 | 34689 | 34226 | 0 | shared_cohorts |
| 025 | 2650833 | 1218240 | 95195136 | shared_cohorts |

共享组路由在这些图上有明显退化，007/025 存在大量 spill，值得后续结合容量和流水顺序研究。此处仅报告同时观测到的事实，未通过消融证明 spill 是唯一原因。完整逐图结果、最终每核 pipe 忙时/内部空闲、COPY 数及路由元数据见 `measurement-audit.json`；不从共享 DDR 下的 COPY 持续时间推造每核搬运字节。

## 可复核产物

- `board-feed-20260924T162629169901Z.json`：30 records / 30 eligible / 0 failed，配套 preflight 退出码 0。导入预检只核格式与可用原件，不增加评分。
- `measurement-audit.json`：60 份 gzip 的完整原字节及压缩字节哈希/长度/mtime=0，13 个源码的工作树及 Git 固定字节，180 个 feed 引用、213686 个 trace 操作最终区间、30 份单核结果原件及身份，全部通过。
- `audit_saved.py`：纯标准库读取已保存文件，不导入构造器或官方调度函数，不调用 solver/E0/E1/E2；可从仓库根执行 `.venv/Scripts/python.exe -X utf8 -B results/a/q2-yuanzhifang/feedback-20260924/round5a/audit_saved.py`。
- `ledger.json`、`spec.json`、`summary.json`、`summary.csv` 及每图 plan / run / 完整 result、trace、log、stdout、stderr 均保留。

本次写入目录的只读元数据扫描未发现 `._*`、`.DS_Store` 或 `__MACOSX`；Windows 不运行不存在的 `dot_clean`。工作树其他未提交文件属于父任务范围，本审计没有改动。提交后的 Git 存储字节核验由父任务在冻结提交后完成。
