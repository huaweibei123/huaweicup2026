# P2 full100 B 批测量与原件审计

本批按固定协议唯一运行一次，30 个四核 `tensor_packet` 用例均成功。C 与 round6k1 未在本批执行；以下部分样本均值不能替代全部 100 图均值。

- Spec：`e6deb8a78b45bda186577b853390fe7632354c2b`；solver：`e64723bdf99669c44f76d8e90ab0379a8578522e`；runner：`fa6522a3266fe040379dd064092beb27c0b20a5e`；E0：`45f647b395b84e9569f418fd33d62c2b8eb4d190`。
- 实际 T0：`2026-09-24T16:47:05.106436Z`；T1：`2026-09-24T16:50:21.169583Z`；批次墙钟 196.063242 秒。批前固定字节检查另计 4.955489 秒。
- 账本实际 30 solver、30 E0、0 E1、0 E2；单 worker，零重试。60 个阶段起止区间无重叠。结束后即通知父任务释放评分窗口。
- solver 总墙钟 39.113021 秒，中位数 1.074077 秒，最大 4.167209 秒；独立 E0 总墙钟 136.523337 秒。
- B 的 30 图逐图单核/四核比值算术均值为 **2.893829107**。复用同一 solver 提交的 R4 十图与 A 三十图后，70 个互不重复用例均值为 **3.149605515**。仍缺 C 的 30 图。

每次 solver 为新 Python 进程，墙钟覆盖启动、输入读取、在线结构处理、方案构造与落盘、退出及父进程持久记账开销；外部 E0、压缩和报告处理另列。OS 文件缓存与普通系统负载未控制，峰值 RSS 未测。按父任务协调消息，P3 前一批已于 `2026-09-24T16:44:41.928363Z` 完成，P1 等待 B 的实际 T1；此信息不替代整机进程采样。

## 可用于下一轮的反馈

相对于旧连续分配固定来源 `6664a63adc3464d28d1f835d907cdeaea23e6b35`，本批 28 图改善、2 图退步。图、配置、官方源码身份均一致；该旧来源不是中央当前最好成绩。

| 用例 | 当前 cycles | 旧连续 cycles | 分区额外 bytes | spill bytes | 路由 |
|---|---:|---:|---:|---:|---|
| 036 | 503212 | 233584 | 3456 | 11223040 | shared_stages |
| 037 | 53852 | 53320 | 614400 | 0 | packet_eft |

036 的大量 spill 与退化同时出现，037 则是无 spill 的搬运增加，不能用单一容量解释覆盖全部退化。其他较弱加速比包括 068 的 1.238419、066 的 1.452267、050 的 1.452497，均走 `shared_cohorts` 且 spill 为 0；需结合每核 pipe 和依赖结构继续研究。

044 实测 44234 cycles。051 为 206387，比已核固定队长六图来源 `5e5d688ca473a80b7d72b26b397ce568ac70f534` 的 207134 少 747 cycles；不据此宣称超过中央当前最好。062/063 分别为 771798/280084，低于本机 R3 的 847213/400563；不引用 P3 treecut 值作为 P2 对照。这些均为事后研发比较，不是在线 portfolio 选择。

## 可复核产物

- `board-feed-20260924T165049996927Z.json`：30 records / 30 eligible / 0 failed，配套 preflight 退出码 0；导入预检不增加评分。
- `measurement-audit.json`：60 份 gzip 的 raw/stored 哈希、长度与 mtime=0，13 个工作树/Git 固定源码，180 个 feed 引用，239799 个 trace 操作最终区间，30 份单核结果原件与身份，全部核对通过。
- `audit_saved.py`：纯标准库读取已保存文件，不导入构造器或官方调度函数，不调用 solver/E0/E1/E2；从仓库根执行 `.venv/Scripts/python.exe -X utf8 -B results/a/q2-yuanzhifang/feedback-20260924/round5b/audit_saved.py` 可重做。
- `ledger.json`、`spec.json`、`summary.json`、`summary.csv` 与每图 plan、run、完整 result/trace/log/stdout/stderr 均保留。每核 pipe 忙时和内部空闲使用最终原件区间，不从共享 DDR 下的 COPY 持续时间推造每核字节，也不自动认定瓶颈原因。

本次写入目录的只读元数据扫描未发现 `._*`、`.DS_Store` 或 `__MACOSX`。Windows 不运行不存在的 `dot_clean`。父任务其他研发文件未改动；A 目录保持冻结。提交后的 Git 存储字节核验由父任务在冻结提交后完成。
