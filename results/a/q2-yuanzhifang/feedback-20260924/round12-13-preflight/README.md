# 批次标识预检拒绝与修正

`db44533f5d9f8538697ea3d3c59625fe1763a26f` 的四份 round12/13 spec 把 variant 中的下划线带入 `run_id`。runner 的既有正则只允许小写字母、数字和连字符，故12A、13A第一次命令均在 `validate_spec` 以退出码1停止。两次没有创建批次目录、没有solver或E0调用；12B、13B未启动。原命令与可见错误见 lane-a.txt/lane-b.txt，未补造运行时间，旧spec字节保留在原Git固定提交。

根会只将四份 `run_id` 中的下划线改为连字符；源码、方法、输入、核数、输出目录、24次solver/24次E0上限及停止条件全部不变。四份修正spec逐一执行 `validate_spec` 与 `frozen_inputs`，全部通过，每份核验16个冻结源码文件，0solver/0E0。新spec固定提交后才重新授权子代理开始；这不是评价失败后重试选分，也不增加既定实验调用预算。runner的0自动重试约束保持。

后续批次报告应引用修正后的spec提交，并保留本预检事件。预检成功、正式运行完成、原件审计通过、中央接收仍分别核对。
