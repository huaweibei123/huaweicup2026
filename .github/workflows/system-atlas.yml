name: System Atlas skill

on:
  push:
    branches: [main]
    paths:
      - '.agents/skills/system-atlas/**'
      - 'scripts/rehearsal_*.py'
      - 'tests/rehearsal/**'
      - '.github/workflows/system-atlas.yml'
  pull_request:
    paths:
      - '.agents/skills/system-atlas/**'
      - 'scripts/rehearsal_*.py'
      - 'tests/rehearsal/**'
      - '.github/workflows/system-atlas.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  windows-compat-candidate:
    # OS-level regression only; this does not prove any teammate's machine,
    # browser, GitHub credentials, or live rehearsal connection works.
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: .agents/skills/system-atlas/package-lock.json
      - run: npm ci --ignore-scripts --prefix .agents/skills/system-atlas
      - run: npm run test:windows-compat --prefix .agents/skills/system-atlas
      - run: python scripts/rehearsal_smoke.py
  atlas:
    strategy:
      fail-fast: false
      matrix:
        # Upstream 0.5.0 native Windows fails directory fsync; see rehearsal/VALIDATION.md.
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: .agents/skills/system-atlas
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: .agents/skills/system-atlas/package-lock.json
      - run: npm ci --ignore-scripts
      - run: npm test
      - run: node bin/system-atlas.mjs validate examples/service.system.json --repo-root . --json
      - run: node bin/system-atlas.mjs validate ../../../tests/rehearsal/system.json --repo-root ../../.. --json
      - run: python scripts/rehearsal_smoke.py
        working-directory: .
