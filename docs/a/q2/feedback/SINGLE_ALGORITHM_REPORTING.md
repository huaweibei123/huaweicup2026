# P2 固定算法与完整覆盖

本会话 `yuanzhifang30-sudo/s-eb28fa11a5664fdfbdd29b3d6e38ca24` 已实际 fetch 并阅读公共规范 `5e626d869709578fbe4385e30709d7a2337e9aa9` 的 AGENTS 核心目标、`docs/benchmarks/BENCHMARK_BOARD.md`，以及 Issue #26 评论 5819178999、Issue #33 评论 5819232165。保留当前成果，不切换或覆盖他人源码。资料已读不代表队长验收。

当前正式完整实测仍是同一 `e64723bdf99669c44f76d8e90ab0379a8578522e`、统一 `python -m src.q2.feedback.construct --strategy tensor_packet` 入口，全部100图×1–5核、固定配置、无在线评价。数据固定在 `384b6c2a7ff937ca44180dee09a9d4bcaea0c50d` 的 `results/a/q2-yuanzhifang/feedback-20260924/full-coverage/all500/`。1–5核逐图 B/M 算术均值分别为 1.056973404、1.938126019、2.673479984、3.330228459、3.896036904。所有失败和额外调用仍在原始账本中，501次solver/500次官方E0的差异有独立事故记录。求解端到端墙钟与外部 E0 墙钟分列，5–10分钟是效率建议而非硬淘汰线，算法构造不能靠限制迭代次数自称避免暴力搜索。

源码 `384b6c2a7ff937ca44180dee09a9d4bcaea0c50d` 的 capacity_window 与 gap_packet 是分别固定的机制试验。前33项数据固定 `2428372b52a1eeb86dd5fed08ce33346ae79e6fc`，未构成任何新算法全500证据。后续24项2/3核试验按 NEXT_PILOT_DESIGN 与四份spec提前固定，用于机制泛化与负对照；这份协议通知本身不增加实验预算，实验沿用用户持续P2研发授权。

每格历史赢家与试验后的事后包络只用于找瓶颈；它们不作为正式算法均值、论文主结论、用户数值目标达成或与队长路线胜负的证据。下一次正式比较将冻结同一完整solver SHA、统一入口、按输入结构自动选择的规则及预算，建立其完整500格来源。程序选机制可以不同，但不能离线按case赢家表选择。

旧E0要复用，必须逐格核同原始方案字节、输入图、配置、核数和评价语义并标明来源；仅语义相似、同样的Makespan或同一方法名不够。新版求解墙钟须单独实测，不能挪用旧值。中央签名接收、镜像显示、完整覆盖、科学复核分别记录，不能从全台总条数推定某个solver已收齐。
