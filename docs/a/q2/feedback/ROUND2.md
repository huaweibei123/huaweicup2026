# P2 第二轮：044的收益与资源词反例

## 结论

只翻转窗口内并列优先级，在044/4核得到 **66901 cycles**，solver新进程端到端 **0.763362秒**，独立未修改官方E0 **1.323193秒**；spill从首轮窗口法的1262688字节降到0。相对首轮91646降低27.00%，相对本机当时成绩台已有69113降低3.20%。同时其余四图全部退步，**不能把fill作为通用替换或宣布全库最好**。本轮证据支持研究共享数据的阶段顺序，反驳“仅让瓶颈M顺序相同即可消除气泡”的简单解释。

## 五个事前固定单元

| 图 | 首轮window | 第二轮fill | 变化cycles | 已有word或其他结构对照 |
|---|---:|---:|---:|---|
| 008 | 64806 | 66876 | +2070 | word63768 |
| 095 | 270724 | 272794 | +2070 | word269686 |
| 084 | 318552 | 319668 | +1116 | word317988 |
| 044 | 91646 | **66901** | **−24745** | 旧M2 69113；协作者stage67339 |
| 080 | 111466 | 113498 | +2032 | 协作者shared_place87353 |

原始run/result/trace和标准feed位于 `results/a/q2-yuanzhifang/feedback-20260924/round2/`。本轮5构造、5 E0全部成功；两轮累计25构造、23 E0、2预期unsupported，0意外失败/重试，0 E1/E2。每轮独立事前spec，没有把剩余额度转成搜索。

044 fill的scheduled COPY为3766656字节、总extra2791200字节，全部extra来自分区，spill为0；原始图COPY975456字节。080 fill新增spill229376字节，总extra4419584字节，计算阶段交错带来的驻留压力会退化。

## 为什么008反而更差

以下只分析保存的E0原件，无新评价：

- 008四核中，fill的 **M操作ID顺序已经与已知word相同**，M总忙时、首次开始也相同。core0仍在4298～7406出现3108 cycles内部空闲；word没有内部空闲，旧window在末段有1038空闲。
- word的V队列先连续完成作业0的6～11号操作，op11结束4178。fill在V op9后插入其他作业的54、55、56、102等操作，直到6824～7406才运行op11；依赖它的返回M op12必须等到7406。
- 因此，M优先级词相同不足以保证同一Makespan。配套V顺序和COPY/内存依赖的释放时间也必须保持。将“剩余工作优先填充”施加到所有Pipe，会分散第一个作业的V阶段而使M提前等待。首轮的尾部空闲消失并不是成功标准；它可以转移并扩大到前段。

审计原件：`round2/measurement-audit.json`。所有策略的有序mapping、操作分核、默认W与首轮保持一致；不是通过改变核心归属取得此差异。

## 044的机制证据与界限

core0两种窗口法M忙时均20292、V忙时均2724；旧window的MTE2忙时81939，存在20段内部空闲，总计7940。fill的MTE2从0连续运行至62505，没有内部空闲；该核心M结束66752、最后COPY_OUT结束66901。全图spill从1262688降到0，而partition_added_copy_bytes不变。这与更同步地消费各分量共享输入、降低重复驻留/溢出的解释一致，但**没有把一例时间线升级为充分结构守卫或普遍定理**。

下一步应先建立可检查的结构条件：重入M–V–M族保留完整M/V资源词；共享长链族分析同层消费者与张量生存期；分量核心分配单独处理。不能按case ID硬编码这五格的事后赢家，也不能据此将其称为已测的在线portfolio。本轮不继续枚举W或tail权重。

## 计时与验收边界

算法固定 `3e6cbe2a9d6f50ed13a236f9a9a2b098e343f7fc`，runner仍 `fa6522a3266fe040379dd064092beb27c0b20a5e`，spec `d898c1ada76057fe3f328a8adfd58bdfb034c047`。UTC T0 `2026-09-24T14:32:43.781757Z`，T1 `14:33:07.238920Z`；批次23.457400秒，前置原件/Git预检另计4.298916秒。每个进程30秒上限、总240秒、末15秒收尾、单worker、不重试。已与P1/P3的E0错峰，不控制其他交互应用/OS缓存；未测peak RSS。

6项结构检查通过，5份官方E0结果均成功，workspace feed预检5/5 eligible；10份gzip核原始字节。成绩台的fixed-Git核验、实际接收与页面读回分别留回执，不用本机“成功”替代中央接收或独立复跑。只有5张公开图的4核数据，尚未进行100图/1～5核或尾延迟统计；5～10分钟不是研发完成门槛。

## 与P3协作的独立证据

同机P3 session `s-3d9c78db26714786b88b987ca6f58e2b` 已独立完成044同构链按算子位置分层：P2 67339、P3 66992，固定[5f137e1原件](https://github.com/huaweibei123/huaweicup2026/tree/5f137e14f5bc73d123c53401737e2b5dc62e66d2/results/a/q3-yuanzhifang/stages-20260924)。本P2 fill较该P2结果低438 cycles（0.65%），该比较来自各自原件/收据，不是本会话对对方算法的独立复跑；其P3结果不能拿来替代本P2评价。双方实现和作者归属分开，互报固定证据以研究优先级差异。

交付后通过 `git show 5f137e1:<path>` 读取对方plan/run/完整P2结果，核对所存plan/result哈希及graph/config/official身份全部一致。双方有序mapping、各核subgraph集合完全相同；全部COPY字节与零spill也相同。core0～2的MTE2都从0连续到62505；区别发生在计算/输出收尾：stages最后M结束66980、最终67339，fill最后M结束66752、最终66901。因此这438 cycles的差异并非少搬数据或重新分核，后续应比较各计算Pipe的收尾优先级与输出依赖。见结果根目录 `peer-044-comparison.json`；此审计0 solver/E0。
