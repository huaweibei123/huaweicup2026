# gap_packet 剩余 275 格恢复冻结

原计划固定于 `f2dd60157fe9268ea86e44f57c94c2569e044829`。round14d/e/f/g/h/i 共184个新格已成功；原同源码29格保留。round14m 的实际 UTC T0/T1 为2026-09-24T19:11:42.656781Z / 19:13:13.975415Z，13 solver /12 E0：前12成功，014/k5 solver在30.0092796秒触发30秒外部watchdog，未调用E0，其后20项未启动。原账本停止、进程退出、零自动重试，失败原件不修改。独立停止批审计验证12个成功结果、24个gzip和57785个trace操作区间。

本轮是根会针对用户持续补齐P2任务制定的单独恢复预算；不是将原471上限偷偷改写为成功。原471格补测阶段累计最多472 solver /471 E0，额外1次solver是上述保留失败。原29格计入整个gap矩阵时，总成本最多501 solver /500 E0。停止批12项通过原件检查后原样复用，不重复评分。

新的9个round15规格补275格：A流水线j/k/l/b为33/33/28/33项；B流水线m/n/o/a/c为21/33/30/33/31项。round15m仅含014及原未启动20项。原未启动的round14a/b/c/j/k/l/n/o规格保留且不再启动。详细图号、原spec与调用上限见`gap-full500-recovery-plan.json`。

统一将这9个新批的外部solver watchdog设为60秒；E0 90秒、batch900秒、cleanup15秒、每批1worker、总P2最多2worker、批前free RAM≥1.5GiB、首失败停、零自动重试。新阶段上限275 solver /275 E0。若再失败，保留现场重新判断，不能自行继续增加预算。该调整容纳更高核数的k²构造开销及共享主机扰动；现有证据不能把超时唯一归因为其中任何一个因素。

算法仍为源码`384b6c2a7ff937ca44180dee09a9d4bcaea0c50d`、统一`construct --strategy gap_packet`、相同固定参数/输入/配置。runner watchdog只控制外部进程终止，不传入solver；这五份构造文件没有依时钟/截止时刻切换候选的逻辑。不能把原30秒批次改写成60秒运行：每条原收据的watchdog和实际墙钟完整保留，成功测量的方案/结果字节也不变。整个合并报告是同一确定性算法的覆盖报告，但外部watchdog批次不完全相同；不把它描述为完全同预算、同资源的时延对照。

`gap-recovery-preflight.json`已只读核验9份规格、涉及98个不同原图及16份冻结源码，0 solver /0 E0。启动须引用包含上述规格的实际固定提交。仍在使用的五份算法源码和measure/export/analyze/audit_batch保持冻结。
