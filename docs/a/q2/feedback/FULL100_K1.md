# 同一新构造的全100图单核验证

2026-09-24T16:24UTC 本机镜像的中央已核数据中，P2单核100格都只有旧 `q2-contiguous-baseline` 的一次记录，其已测 Makespan 恰好等于固定官方单核基准，故每格 B/M=1。成绩台并未把单核算法的比值硬编码为1；新版算法可以通过合法的分块/顺序减少周期，但是否改进必须重新评估。此问题是实验覆盖缺口，不修改分母、公式或历史结果来消除1.000。

复用四核正在全量验证的同一源 e64723bdf99669c44f76d8e90ab0379a8578522e：`tensor_packet`，参数只改 `cores=1`。不把旧单核 E0 作为新方案结果，仍用其对应的固定官方 A 单核分母。1核只允许输出一个 core_schedules 条目，全部eligible操作均须覆盖；无跨核COPY不会使spill、COPY端点或多pipe重叠自动消失。

事前固定 `round6a/b/c-spec.json`，全部100图各一次冷启动solver及独立未改E0，33/33/34分批；先覆盖既有结构类别（资源词008/084/095、共享输入044/028/067/073、大连通分量002/016/064），其余按图号递增，不用运行结果动态挑选。该顺序只决定资源窗口，不用于报告有利子集均值。

每批1 worker；solver30秒/E0 90秒，批1800秒含15秒收尾，0重试、首个意外失败停止。三批必须分别取得明确START，核对同机其他会话实际T1；禁止自动连跑占窗。若超时、失败或尚未运行，逐图记缺口，只有100/100同身份成功后才报告本算法完整均值。不得把“当前最佳混合方案”的覆盖均值冒充同一新算法全量结果。

solver端到端包含新进程、读图、构造、校验、落盘及退出；外部最终E0与离线导出/复核另列。Windows/Ryzen5600H，普通OS/交互负载与文件缓存未控制；单次进程冷启动不是严格冷缓存实验，不作跨硬件速度结论。方法不变，新增总预算为100 solver + 100 E0，而非候选搜索。该批完成后仍需2/3/5核以及改进算法的结构研究；完整单核成绩不代表各指标超越或已接近可证明最优。
