# 067/k5：完整作业归核的不可超越界

固定只读驱动 `b64397b41876da410758bdd1ec5d0553d507ff23:src/q3_yuanzhifang/whole_job_bound.py` 独立解析官方原图，不使用候选构造、derive或Step。原始COPY均为源/汇端点，故eligible计算节点之间不存在漏掉的COPY桥接；以直接操作边和有eligible生产者的张量边建立计算连通分量，得到71个等工作量分量，每个124个计算操作、PIPE_M=829792周期、PIPE_V=33248周期。逐分量节点集哈希、图/配置/E0身份及成员成绩台记录保存在 `whole-job-bound-067-k5.json`。本次0solver/plan/derive/Step/E0/E1/E2。

若每个完整分量只归一个核心，71个分量放到5核，由抽屉原理至少一核承担15个。该核心只有一条M管线，其必要计算服务量是 `15×829792=12446880` 周期。因此任何完整作业归核方案的Makespan均不小于12446880；改变wave宽度、Cache命中、COPY次序或理想零搬运都不能越过此M工作量界。它不要求分量在核内连续执行，也不依赖某个候选的FIFO次序。

21:04:15Z成员成绩台实际读取同graph/config/E0身份、5核、P3且eligible的当前记录：12237901周期。相同值已出现在expanded/calendar/witness三个固定版本；回执选取较新sequence的记录并保留原记录字段。这是成员镜像接收的官方结果元数据，未独立下载并复评其全部原件。完整作业界高出208979周期（约1.71%），所以本地两个whole-job wave模式均不可能超过这个已有Makespan。

第十六wave批在首派前因内存停止，0调用；独立a023787a2补测已冻结并零调用预检，但在发现此界后取消启动，没有生成新运行账。这样节省的是原计划2次冷构造/4次外部E0，并非把它们记作成功调用。累计实际仍33cold/56外部E0。此剪枝仅针对超过当前067/k5主指标的目标，不证明whole-job模式在DDR或求解墙钟上没有Pareto价值，也不证明所有切分已经最优。

当前全图head-tail必要界为11785739，原固定源码09abe6b0已有存档；现有12237901与它仍有452162周期差距。下一候选因此只切分余下的第71个作业：前70个仍完整、每核14个，最后一个按计算工作量的连续DP切成五段，减少整作业不可分割负载。新增的跨核依赖、COPY、spill与Cache代价尚待官方验证；降低一个工作量下界不自动降低Makespan。方法实现与实验在独立来源下记录，不把未测候选加入正式成绩。
