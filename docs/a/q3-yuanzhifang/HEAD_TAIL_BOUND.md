# 原图最早开始／最小后续长度的区间工作量界

补充适用域与追溯状态见BOUND_SCOPE_REVIEW.md：新实现显式要求原tensor至多一个producer。已复用固定P2结构审计，逐图SHA与本分支100图及既有500行结果全部一致，100/100满足该条件；没有重扫原图或重算旧界。条件核验与LB≤T一致性检查分开记录。

适用于冻结官方模型中非COPY的原M/V操作，时长为非负整数cycles取max(1,cycles)。每个原操作必须恰执行一次，每核每pipe一个槽。只使用官方直接op边及原tensor的eligible生产消费边；不使用未知分核的500延迟，不把COPY收缩边视为必有依赖。

给每个原操作u计算head h_u：忽略资源争用的最早开始；tail t_u：该操作结束后到全部图完成至少还需的长度。原计算边u→v给h_v≥h_u+w_u、t_u≥w_v+t_v。没有原logical_tid别名时，原图输入第一次冷填充的最小COPY时间可作为消费者head起点，必需最终输出COPY时间可作为生产者tail起点；理由同LOWER_BOUNDS.md，Cache必须先完成首次miss才能被别核命中。有别名时禁用这些IO起点，只保留计算依赖。

对某一pipe p、任意阈值a,b，定义非空S={u: pipe(u)=p,h_u≥a,t_u≥b}。每个u必须在区间[a,T−b]内执行，k核该pipe在此区间最多提供k(T−a−b)工作。因此：

    T ≥ a + b + ceil(sum_{u∈S} w_u / k).

最大化所有阈值并对pipe取最大，仍为**任意合法方案**的必要下界。该界不是固定方案FIFO界；它不选核、不生成plan、没有调用E0。更强的必要界不代表可同时达到，更不证明当前构造已最优。

## 完整阈值族的高效求解

非空S只在某个head/tail值处改变；在两相邻阈值之间提高阈值直到下一个实际值只会加强同一S的界。因此只需考虑实际h和t取值。按head降序插入操作，按tail升序建立线段树。每个节点代表候选b，值为 k*b+已插入且tail≥b的操作工作和。插入(h,t,w)给所有b≤t做前缀加w；处理同head的整组后，查询b≤当前最大tail的前缀最大值，保证S非空。该值除k上取整后加a就是本组最强界。

每个操作一次更新，head每组一次查询，故每个k/p需要O(V_p log V_p)时间及O(V_p)空间。k≤5，全部head/tail路径在O((V+E)log V)确定；不做O(V²)阈值对暴扫。记录达到最大值的a,b、操作数和工作和作为可独立检查的见证。

测试用完整阈值对暴枚举做小规模独立oracle，并核验15节点单位完全二叉树4核：普通ceil(W/k)=4，而8个叶子必须在至少3周期后续路径前执行完，新界为3+ceil(8/4)=5。这个例子说明改进来自必要的收尾结构；不是官方用例成绩。

## 100图运行及局限

固定源码 `09abe6b043d4292e091985151232b5b2861cb07d` 后扫描100图×1–5核，0solver/0E0，1个低优先级worker、180秒watchdog。实际2026-09-24T16:11:07.254176Z–16:12:02.047734Z，外层54.793485秒，模块含源码/官方身份预检54.188115秒。自然完成，无终止/重试。P2生产者随后确认尚未派发A评分批，不存在与该批评分重叠；此前的窗口交接消息交叉已更正。

原件 [head-tail-bounds-100.json](head-tail-bounds-100.json) 含源码/图/配置hash、500格双pipe见证；[复核摘要](head-tail-bound-summary.json) 含全部旧界、控制记录ID及实际命令/计时。逐格graph/config/official身份一致，控制分母再次直接读取并核SHA的官方单核scene-A原件，没有ratio×T反推。对照沿用15:11冻结中央历史最佳组合；这不是某个新算法的全量成绩。

与旧界取max后，493/500格严格增强，最大单格增强23.574%；500格均不超过原控制T，也不超过044/069/071的新已固定结果。这个一致性检验不替代上述必要性证明。

| 核数 | 严格增强格数 | 旧mean(B/LB) | 新mean(B/LB) |
|---:|---:|---:|---:|
|1|100|1.242988|1.241941|
|2|100|2.483348|2.478270|
|3|100|3.710391|3.699180|
|4|97|4.931894|4.909565|
|5|96|6.109739|6.062786|

这些是乐观加速比上限，并非可达承诺或实测均值。分析发布时044/k4、069/k5、071/k5的合法T与合并LB分别为40927/18715、15221/3835、11613/3098；后续实测进展见FEEDBACK.md。仍有明显理论缺口，不能从其余500格LB≤T宣称无法继续优化。

分析作为研发成本单列，没有加入当前在线solver。若以后把它用于在线选优，必须计入完整solver wall。后续若出现任何LB>T，立即停止使用冲突界并调查。
