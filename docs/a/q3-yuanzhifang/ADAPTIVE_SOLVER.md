# 协作统一入口：共享链流水与队长V2

入口为 `python -m src.q3_yuanzhifang.adaptive_solve GRAPH --cores K --output PLAN --evidence DIR`。本入口按图结构自动决定唯一策略，不读取case编号、历史成绩、预计算候选或结果表。配置、官方E0、所有依赖与入口一起按完整代码提交冻结；统一正式100×5成绩须另行形成，现有机制批次不得逐格拼接成该入口的结果。

2–5核时先检查独立分量数不少于核数、同长2–512位置的真实串行链、每个计算操作均为正整数周期M/V，并保留原M→V*→M等首尾工作且中间工作≤2M的resource-word守卫。随后核共同输入非空、每位置管线/周期/共同输入签名同构；满足时运行本线6bae8dfa的连续阶段DP与singleton、作业优先顺序。只构造一个方案并做一次在线官方P3评价；评价或保存异常直接失败，不伪装为守卫不适用。DP为O(kL²)时间、O(kL)空间，使用纯计算串行阶段模型，未承诺E0最优或跨分支不退化。

其余图与单核保留队长NikolaStarx固定2d5459fa的stage迁移、stage、attention/FFN和guarded策略，最多两次在线E0。12个依赖原件保存在独立captain_v2包，只有根目录层级适配，来源/原Git blob/前后SHA-256逐文件记录；不将队长算法声明为本线原创。共同输入索引、守卫、构造、在线E0、候选保存、最终方案落盘与退出均计入本次solver端到端墙钟。

选用计算均衡流水的现有依据是044/k4=40927。cold-setup修正的抽象最优切分只得到41738，因此暂未加入；阶段合桶为97641，也未加入。三个数字都是独立开发机制，不能当作新入口实测。下一步以六个同构共享权重图的有限批次检验可推广性；必须保留退化与失败，不能以单格收益宣布完整均值改善。

验证使用注入评价回调，覆盖新路由实际构造/单次调用、12节点完整唯一覆盖、原MVM/单核/无共同输入的委派、评价错误传播、零周期拒绝及12个队长模块原字节复原校验。合成构造检查不调用E0，不等于官方数据验证、独立验收或质量保证。
