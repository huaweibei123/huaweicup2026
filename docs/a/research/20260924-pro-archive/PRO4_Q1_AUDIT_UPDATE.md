# 第四路 Q1 审计补档：接收修订与本机定点复核

2026-09-24，调度归档固定 `96efc94d098c5950c7653088104fba02fb864215` 补齐第四路同轮的另一条回答，来源消息 `291b75a4-32a9-4a6f-a562-18febf9e7097`。此前本专项只取得 Q2/Q3 新研究报告，因此“第四路没有提交逐项 Q1 勘误审计”仅适用于当时已取得的材料，现予修订。历史原文和旧审阅保留，不将本补档倒填为早已读到。

## 已读范围与新增材料

已读 `AI chats/README.md`、第四路目录 README、`附件/r03-AUDIT.md` 全文及 `r03-summary.json`，核对 manifest 中的附件身份；同时读取 main `f637fc7` 的归档约定及上述归档提交的 `docs/CHAT_ARCHIVE.md`。没有无差别导入其他路线的新全文。

审计补丁 ZIP 为 168473 bytes、190 个成员，SHA-256 为 `5dca5a51238849c681d5aed3160265e192c0a179168a021ce539fc153f2e2b96`；本机重新核对 SHA 与 ZIP CRC。`AUDIT.md` 是 32951 bytes，SHA-256 `60b47cb4fe1519deb50e3c3b878d774c34a9a8a93aae40cc14d4e4b59148a456`。原包位于固定归档目录，不重复复制或执行其脚本。

新报告确实交付了逐项影响矩阵、Q1 可提交自由度、Task 内存/边界分析及新的语义反例。**审计材料的交付缺口已补齐；全部数学和实验的独立验收尚未完成。** 作者的 17 次 CLI、117 份原记录绑定及240份标签核对继续归为作者报告，本机复核范围仅如下三次。

## 本机复核

`src/q1/audit_patch_review.py` 从固定 ZIP 提取三个指定 graph/plan 的原始字节，分别运行未修改官方 Q1 CLI。另在单独的 Task 构建调用中透明观察 Step2 输入与输出，以保留真实边界图、Step1 序列和 spill records；没有执行下载包的 Python 程序。冻结源码逐文件前后哈希相同，三例观察的搬运五字段与 CLI 一致。

| 合成输入 | 本机 E0 makespan | 关键观察 |
|---|---:|---|
| projection_false_negative | 13134 | 完整 Step1 的 L1 闭区间峰值524292；删除COPY后的错误计算投影393219；容量524288；实际1条spill、262146 bytes |
| internal_split_backed | 48203 | logical tensor10002两次回读均使用 original_copy_in，无新写回；全图边界新增524288、spill1572864 bytes |
| internal_merged_unbacked | 43729 | 同一原图合并后，10002第一次 new_spill_out，第二次 reused_spill_out；边界新增0、spill1835008 bytes |

两个合并对照的 graph 字节相同。10002均为L1且换入版本递增至1、2，大小262144。合并节省524288边界字节，却多262144 spill字节，净 scheduled COPY只减少262144。该例合并依然更快，不能据此制定“失去初始backing就禁止合并”的规则。

这是三次**合成语义实验**，不并入四例180次正式case邻域评价，也不作为候选质量提升成绩。区间峰值是对真实 Step1 的无spill反事实，不是执行后的物理峰值；这里只复核三个实例及相关源码分支，不是对完整引擎所有字段再次差分。

结果、原输入、独立 CLI 输出、Task 观察、运行源码快照和依赖身份在 `results/a/q1-pro4-audit-20260924/`。复跑：

```sh
uv sync --locked
uv run python -B src/q1/audit_patch_review.py --output results/a/new-pro4-q1-audit
```

本地 Git 对象库须包含归档固定提交；输出目录须不存在。脚本使用当前 checkout 的冻结官方原件，其哈希全部写入协议。

## 对当前 A 实现的影响

1. **当前成绩和合法移动区间无需作废。** `78105c7` 的移动区间只证明联合 Task DAG 合法，全部候选仍用精确事件模拟；构造中的内存压力没有被当成零spill硬剪枝，合并后的局部图也重新交官方编译。因此这三个反例没有击穿现有实现中已使用的假设。
2. **实际 Task 图的零spill判据可以重新评估，不能仅限于原 singleton 描述。** 新报告基于“首个溢出以前没有spill分岔”的论证，与本机读到的官方 alloc→检查→free 分支一致。对象必须是重建边界后的完整合法 Step1 序列，包含 COPY、使用 Task 本地 pos；结果须区分成功空记录与分配异常。当前还没有把该判据加入主搜索剪枝；证书失败不等于方案非法或较慢。
3. **后续拆分/合并估价必须重建 backing 和序列。** Task输入转成内部计算结果时，初始 COPY_IN backing 可能消失；不得拼接旧峰值、按每次spill固定双向字节，或把边界字节减少当成总搬运与时延减少。三个定点复核已提供可回归输入。
4. **保留固定分区的缓存边界。** 新报告确认核心归属/核序改变不影响局部编译输入，但分区变化可能移动后续全局生成ID。现有 evaluator 使用固定版本保守键；不因报告而改成仅按 Task 成员集合命中。
5. **新下界与拆分切点先列为待实现建议。** 计算工作加 Task 完成屏障下界、真实 Step1 压力处切分值得用于下一轮；当前没有验收其全部边界或将其接入硬剪枝，也不据此扩大当前成绩范围。

另已读取原生 evaluator 固定 `03f02e79de4b4bd6f55241385664b154f4332454` 的 `research/a/native-replay-20260924/HANDOFF.md` 与 PR38。其33.66倍来自单图64候选的内部评分口径；生产回退、独立多图、RSS/长跑和完整启动/IO尚待工程化验证。A仍使用固定E1，不抢占LYX写范围，不把研究倍率换算为A端到端加速。

任务六字段：目标为修正第四路材料接收状态并验关键Q1语义；输入为固定补档及冻结官方代码；输出为本接收修订、独立三例复跑器及结果；限制为三个合成输入、未全面独立验收报告；验收为原件哈希/CRC、三次CLI成功与搬运字段/关键spill记录核对；节点为纳入A设计依据，继续与正式case成绩分离。
