# P1 精确批量评估机制交接

这是一份专项会话完成有限实验后交给 FAST 的研究材料。发布只确认文件身份与完整性，不表示队长已独立重跑或正式实现达到验收门槛。原 FAST、E0、任务卡与验收指标均未更改。

先读 [交接报告](ACCELERATION_HANDOFF.md)，再取 [v2 传输包](evaluator-acceleration-probe-v2.zip)。[清单](HANDOFF_MANIFEST.json) 含包内 38 个报告、脚本和证据文件的哈希；[发布回执](PUBLICATION.json) 固定整个 ZIP 的身份。只使用 v2。

报告的 3.885× E0 来自 P1 的一个图、固定划分、12 个不同重调度候选，三轮均冷缓存；随机划分仍有零命中负例。内存须按整个进程/父子进程统计，8.49 MiB 缓存载荷不是 RSS。完整 CLI/Trace、P2/P3、正式大图 spill、跨平台和封存验收仍待验证。

## FAST 首个交接节点

1. 保留原工作，在本人独立实验目录核对 FAST 基线 `d83d5f32a1c23f6450aa9c15fa85891c4ebddd7f`、冻结原件与包哈希，阅读报告及失败范围。
2. 在该基线 worktree 的 `output/<独立运行名>/` 解压材料作为只读参考；将包内 `probe/*.py` 复制到另一个同深度的 `output/<新复跑名>/probe/`，不要把已有 JSON/log 混入新输出。脚本从自身路径推导仓库根，勿直接在本研究文档目录运行。
3. 按报告命令和既定 CPU/内存预算进行独立复现；先交固定输入、完整输出差分、实际计时范围、内存与负例。Windows 差异照实报告，不以 Mac 比值替代本机结论。
4. 复现/边界核对后，在本人 FAST 分支分项工程化，逐项保留消融与回退。原型的运行时字符串替换不进入正式实现；不缓存全局答案，不直接把 P1 key 套用到 P2/P3。

算法取舍与停止条件、完整六字段任务见交接报告。若复现失败或收益不足，保留反例和已有成果，带最小失败输入回 Issue #15；不循环微调凑倍数。原任务的泛化验收门槛保持不变，先独立复现再逐项集成。
