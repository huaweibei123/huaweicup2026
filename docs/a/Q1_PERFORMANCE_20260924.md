# P1：从固定切块转向图结构构造

本轮目标同时降低未修改官方 E0 的 Makespan 和生成合法方案的端到端墙钟。
当前主构造为 `src/q1/bounded_tasks.py`，固定实现
`05f8fa0f7e52f5914f14815f6bdbcb851b631556`，参数
`packet_factor=4, trigger_ops=4096, chunk_ops=1024`。
它从原图直接生成方案，不读取既有答案，不在线评分、训练或枚举候选。

## 已验证的四核全量结果

100 个官方用例全部 E0 成功。统计来自同一实现、同一参数的 99 个新尝试，
加已保存的 014 同实现尝试；没有把历史最优方案混入这一均值。

| 指标 | 旧 fixed64 | 本轮直接构造 |
|---|---:|---:|
| 平均单核加速比：逐图 singlecore/Makespan 的算术平均 | 1.088051407 | 2.949082318 |
| 比官方单核更慢的用例数 | 50 | 0 |
| 对 fixed64 的逐图比较 | — | 100 胜、0 平、0 负 |
| 对官方单核的逐图比较 | — | 87 胜、13 平、0 负 |

新构造实测启动至合法方案落盘、诊断收尾及进程退出：中位 **0.07746 s**、
跨用例最近秩 P95 **0.56895 s**、最大 **0.83685 s**。外部最终 E0 独立计时，
中位 0.89093 s、最大 53.03720 s，不计入一个没有在线评分的构造器耗时。
环境为 Apple M5 Pro、48 GiB、Python 3.12.13，单 worker，解释器逐例新启动，
OS 文件缓存未清空，主机非独占。以上是本批观察，不是重复测量的尾延迟，
也不据此对旧版或队友 Windows 数据声称受控程序提速倍数。

| 用例 | fixed64 Makespan | 新 Makespan | 补充说明 |
|---|---:|---:|---|
| 002 | 255464 | 73544 | 独立归约子树前沿 + 一个剩余尾 Task |
| 014 | 9393843 | 4517065 | 巨大独立分量组拆成 36 个 Task |
| 020 | 1078404 | 133704 | 保留 288 个独立计算分量，分配到 4 个 Task |
| 044 | 131407 | 124268 | 仍逊于历史 profile-refine 的 114443 |
| 045 | 204380 | 31878 | 本地完整计算分量构造 |
| 051 | 634666 | 607628 | 单连通图回退，仍无多核收益 |
| 097 | 5532446 | 2626908 | 仍有 6553600 spill 字节 |

官方单核程序是一条基线，不是所有单核方案的最优值；部分加速比超过核数
不能单独据此判断错误，也不能解读为真机线性扩展证明。

## 为什么改变构造

fixed64 全 500 格反馈暴露了结构问题：020 的 288 个独立分量被交错切成
110 个存在串联依赖的 Task，最后全落在一个核上。只优化切块大小或加核
无法消除这种构造造成的依赖。Pro r06 的链/分量构造建议给出了方向；本轮
用正式图和官方评价验证具体实现，未把 Pro 自报实验当作本机成绩。

1. **完整分量先分核。** 对官方 COPY 收缩后的计算图求弱连通分量，按各 Pipe
   工作向量平衡到核。同核完整分量并入一个 Task，避免人为切出跨 Task 边。
2. **归约树用独立子树前沿。** 分量不足且每节点至多一个后继时，找按工作量
   截断的最大子树反链。子树包分配给各核，剩余节点进入尾 Task。所有跨边
   均进入尾部，合法性可直接论证；更细前沿平衡负载，但会增加边界搬运。
3. **巨大 Task 沿独立分量边界细分。** 只对超过 4096 个计算节点的 Task，
   把内部独立分量装入目标 1024 节点的块；不可分的大分量完整保留。
   各块留在原核、原位置，保留原 Task 增广 DAG 的块间顺序。

四核 100 图中，97 图走完整分量、3 图走树前沿；27 图进入巨大 Task 处理，
实际增加 Task 的是 12 图，43 个原 Task 被拆、净增 210 个 Task；18 图仍有
不可分的大分量。阈值是工程启发式，不是容量证明或最佳阈值。32 图仍有 spill。

前一版不限制 Task 大小的全量尝试在 014 E0 超过 60 秒后停止；保留
12 成功、1 超时、86 未运行。新 014 完成 E0 用 26.23 秒。由于旧超时过程
没有阶段剖析，不能将差异归因于某个具体编译阶段，也没有旧方案 Makespan
可比较。不同批次的停止政策和失败原件均保留，未用成功新批覆盖旧批。

## 如何判断下一步值得优化哪里

静态下界包含计算关键路径、各 Pipe 负载、必要 DDR 服务及最早释放/最短
剩余路径约束。证明条件见 [Q1_LOWER_BOUNDS.md](Q1_LOWER_BOUNDS.md)，
它不收缩会错误添加 Task 内依赖的 COPY 桥，不把零 spill 当作最优证书。
以 E0 上界 U 和静态下界 L 比较，U/L−1 是相对最优差距的上界：
四核 8 图在 1% 内、15 图在 2% 内、23 图在 5% 内。020 为 0.31%，
045 为 1.18%，097 为 1.95%。这些结论依赖冻结语义与下界假设，尚非证明器验收；
大差距也不证明有等幅可实现收益。

优先处理共享输入/容量压力（044）及非归约树的单连通图。13 个未加速图为
005、016、024、047、048、051、064、069、071、075、082、085、086。
Fang 已报告 chain-wave 对 051 有效，但对 048/071 因等待门开销退化，见
[Issue 98 的完整反馈](https://github.com/huaweibei123/huaweicup2026/issues/98#issuecomment-5816412000)。
因此候选选择必须利用可解释结构与真实门控代价，不能事后逐图挑最好值再
冒称一条通用求解算法。当前没有宣称达到全局最优或通过独立科学验收。

## 同一算法的全部 100×5 质量结果

固定05f8构造已得到500格官方结果。最初500次中496成功、4次外部E0超过60秒；
另开四格诊断，仅补原缺失格，逐字节确认新旧plan相同，全部成功。旧失败与原
496/500表保留，未按多次评分择优。累计该固定方法实际504次solver + 504次E0，
不是一次无失败的500格运行；此前不同算法的小样与停止批次另外计费记账。

| 核数 | 有效图数 | fixed64平均单核比 | 新构造平均单核比 | 对fixed64胜/平/负 | 新solver中位 / 最大(s) |
|---|---:|---:|---:|---|---:|
| 1 | 100 | 0.949320 | 1.002097 | 75 / 0 / 25 | 0.10522 / 0.89636 |
| 2 | 100 | 1.041762 | 1.747292 | 100 / 0 / 0 | 0.08813 / 0.88162 |
| 3 | 100 | 1.073107 | 2.388778 | 100 / 0 / 0 | 0.08129 / 0.89170 |
| 4 | 100 | 1.088051 | 2.949082 | 100 / 0 / 0 | 0.07746 / 0.83685 |
| 5 | 100 | 1.091310 | 3.440854 | 99 / 0 / 1 | 0.08075 / 1.03065 |

四核原批为1worker，其他核原400批为2worker，补测为1worker；表中程序耗时
仅为各批实际观测，不把不同并发与系统负载混成受控扩展性能实验。最大成功
外部E0墙钟66.35681秒，与最多1.03065秒的方案生成时间分开。

负例不能被平均数遮住：k1的084为2509460 cycles，差于fixed64的1776688；
k5的044为132892，差于fixed64的131407。核数增加时也有当前启发式方案退化，
这不证明最优核预算的单调性失效。单核排序、共享输入驻留和大分量仍需研究。

400格原提交 `d63001eb01cc254a20bc60cae5e50b1eb1ee2808`，目录
`results/a/q1-bounded-matrix-20260924/20260924T1459Z-bounded400/`；
UTC14:59:28.047Z–15:16:09.987Z，400solver+400E0、396成功+4timeout。
四格新提交 `a19067b4b1016c7ce4e8ec531c3857a3dfb3abc0`，目录
`results/a/q1-bounded-timeout-20260924/20260924T1524Z-timeout4/`，
`full500-filled-summary.json`与JSON/CSV保留明确补缺规则及全部来源。
实际UTC15:22:34.853Z–15:26:32.071Z；controller墙钟242.338秒还包括T0前
身份核验。每格E0窗口180秒、0重试/E1/E2，新观察如下：

| 用例/核数 | Makespan(cycles) | E0墙钟(s) |
|---|---:|---:|
| 041/5 | 1196379 | 59.05002 |
| 062/1 | 2854809 | 42.47588 |
| 079/5 | 6075976 | 66.33977 |
| 087/5 | 886373 | 66.35681 |

新旧批的并发和超时窗口同时变化，旧过程缺少阶段剖析，不单独归因于CPU竞争。
本轮所有构造研发批次合计533solver+533E0（含原5次失败），另有零评分静态
审计与历史原件导出；这些研究成本与部署时一次直接构造的在线耗时分开。

## 复现与交付索引

```sh
uv sync --locked
uv run python -B src/q1/bounded_tasks.py INPUT_GRAPH.json --cores 4 \
  --packet-factor 4 --trigger-ops 4096 --chunk-ops 1024 \
  --output NEW_CASE_multicore_res.json --diagnostics NEW_CASE_diagnostics.json
uv run python -B -m unittest discover -s tests/q1 -p 'test_*.py'
```

52 项现有 Q1 测试在本轮父工作区通过；它们包括构造图级性质、下界反例与
旧搜索测试，不替代官方 100 图实验。新 plan 仅含题面允许的两个键。

- 99 新记录：`results/a/q1-bounded-full4-20260924/20260924T1439Z-boundedfull4-99/`，
  固定原交付 `ad8903ba8c7bf0dcb96913f5ed23f9ab0bb8ddf9`；runner 为
  `5933f8870b6604226f596f694b2df7d5ae468ed0`，实际 T0 14:38:55.685Z、
  结束 14:45:45.653Z，99 solver + 99 E0，无重试/E1/E2。
- 014 原尝试：`results/a/q1-bounded-probe-20260924/20260924T1432Z-bounded014/`，
  固定 `4d374dc25b5698491ddbb92837789a23a4ad3102`，1 solver + 1 E0。
- 下界静态 100×5：`results/a/q1-lower-bounds-20260924/static_bounds.json`，0 评分。
- 同方法质量审计：`results/a/q1-performance-audit-20260924/k4-quality.json`，
  记录来源哈希、逐图 U/L、时间分位，0 新评分，未发现下界大于实测值。
- 历史三格补证：`results/a/q1-historical-refine-feed-20260924/`，revision 2
  只补原有身份匹配的官方单核分母，历史未知完整求解墙钟继续为 null。

提交使用现有 board-submission-v1，固定源码、run、attempt、revision 与同提交
原件引用。成绩台维护者已补充批次与分片口径（PR99），失败和未运行记录
与成功一起提交。原件准入、中央接收、镜像同步和独立科学验收分别记录。

## 两种新候选的三个机制实例

在上述100图反馈之后，新写了两个独立候选。它们没有改变在途400格的
05f8 固定构造，也没有事后替换全量均值中的三个结果。

- `src/q1/sink_peel.py` 递归剥离只到同一 sink 的完整后缀包，共享祖先递归
  留作前缀，再逆序发出粗波次。包可以包含完整 fork/join，区别于逐条链分层。
  跨包与核内顺序均前向，52项测试中包含所有1024个五节点有序DAG的结构检查。
  仍为单sink的016/024/051不会被此规则解决，完整回退到 bounded04。
- `src/q1/input_windows.py` 在外部输入集合很大的多分量图上按依赖深度切窗，
  组件保留原核。它测试控制输入驻留跨度能否减少重复搬运；输入并集预算
  不表示真实同时存活量，也不证明零spill。可能增加门控与边界搬运。

| 方法 / 用例 | bounded04 | 新 E0 Makespan | Task / 波窗数 | 新 extra DDR / spill (B) | solver / E0 (s) |
|---|---:|---:|---|---:|---:|
| sink / 048 k4 | 222220 | 116460 | 68 / 20波 | 730594 / 0 | 0.12852 / 0.23961 |
| sink / 071 k4 | 18919 | 12078 | 16 / 4波 | 239546 / 0 | 0.07358 / 0.12056 |
| input / 044 k4 | 124268 | 76519 | 20 / 5窗 | 2912288 / 0 | 0.07729 / 0.17891 |

三个观察分别降低47.59%、36.16%、38.42%。044相比旧profile 114443还低33.14%，
旧原件已定点校验封存。小样不证明全域质量保底，不仅凭波数或spill变化作
单因素因果归因。两个run合计3solver+3E0，0重试/E1/E2；得到P3空闲窗口后
顺序使用1worker，和主400格独立。sink实际UTC15:06:49.325Z–15:06:49.915Z，
input为15:06:59.946Z–15:07:00.216Z；run_id中的1509Z是预分配标签，实际时间
以运行收据为准。

固定交付：sink `869393453dc1e0e3ae7c119c0ba3f1d66a35f315`，
`results/a/q1-sink-probe-20260924/20260924T1509Z-sink2/`；input
`3a55d70ed6422dda04bcd0a90deed8d9ee3cd83b`，
`results/a/q1-input-probe-20260924/20260924T1509Z-input044/`。
父会话按固定提交重新预检2+1条原件，均有效；方法详情见
[Q1_SINK_PEEL.md](Q1_SINK_PEEL.md) 与 [Q1_INPUT_WINDOWS.md](Q1_INPUT_WINDOWS.md)。
下一项验证包已固定为 `4e6a5b5e6318af460005ce333952884d230e8bf6`，入口
`results/a/q1-sink-member-package-20260924/README.md`；经成绩台owner统一安排
005/047/064/069/075/082/085/086的k4，总预算8solver+8E0，1worker、首次失败停止。
准备过程0真实评分，已有048/071不重复执行；Windows尚待执行者本机验证。

## 本轮六字段交付约定

1. **目标**：优化P1/A的合法切图、分核、核序；首要减少官方Makespan，兼顾额外DDR，
   同时降低raw图到方案落盘的完整求解墙钟。
2. **输入**：只读 `data/raw/a/official-cases.zip` 的100图、
   `data/raw/a/official/data/config.txt` 与 `docs/a/source-manifest.json` 的固定字节；
   图为公开开发集，非盲测。单位分别为cycles、bytes、真实seconds。
3. **输出**：直接构造源码、仅含两键的plan、完整E0原件、逐图JSON/CSV和标准feed、
   本文的方法与限制、可复现命令；可编辑代码/报告随PR100交付。
4. **限制**：官方源码和硬件配置不改；在线不读取已知答案或评分搜索；实验各自冻结
   范围和超时，不覆盖旧run。与P2/P3协调CPU窗口，无GitHub付费CI、GPU或Colab调用。
5. **验收**：本轮已测范围内E0合法成功、来源与原件哈希可核、图级性质与失败例保留；
   质量和墙钟分别比较。预检/入台不能替代Windows复跑、独立科学复核或全部候选全域验证。
6. **截止**：用户未另设硬截止。每批有限预算见固定runner与manifest，耗尽即停；
   不用“追求极致”授权无限搜索或自动启动后续批次。
