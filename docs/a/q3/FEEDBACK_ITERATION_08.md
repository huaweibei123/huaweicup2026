# P3 迭代08：attention row 同核构造

本协议在该新构造评分前冻结。目标是让注意力row内部的短计算、fan-out及再归约保持同核，降低内部多次500周期跨核等待，同时分散共享连通组件的M/V负载。原图/原ADD次序/原算子不改，不复制Q/K/V。

严格原端口结构守卫识别row，row仅为分核约束，外围为不跨row边界的独占串行链。商图按关键后继工作优先；每个ready单元试每核的原op逐前驱release与独立pipe可用时刻，固定确定性选核，不作多参数或随机搜索。原singleton提交保持，官方P3每核重新派生Task，不宣称Task内容或COPY复用与旧方案相同。代理忽略容量/COPY池/Cache，因此必须用完整未修改E0验证。

首样本082/k4；若合法、比同身份历史703323周期更低且时序/搬运解释可核，则同一冻结源、同一batch T0和600秒额度继续075/k4、085/k4。整个机制批次最多3 solver/3在线E0，单worker、每格90秒、0重试、0新P2/单核分母/E2/云；首失败或无改善即停本批，保留证据后再判断模型问题，不临场更改源或补次数。前三项历史身份先核对，若成绩台期间已有更优同身份方案则以新值报告，不改原预设门槛。

每格保存输入/配置/源码哈希、完整plan/result/timeline/receipt/call账本及从进程启动至证据/方案落盘退出的墙钟。先将静态plan与增强FIFO DAG无环/覆盖核验，静态下界差于已测incumbent时不评分。未证明代理是官方上界、不将更低代理当成功。代码与结果分开固定；不把三图成绩作为统一adaptive入口全500结果。

本轮独立于已封存stage-rotate批次；资源窗口在正式启动前与P1协调，集中大批验收仍交成绩台单写者。结构来源、反例及完整静态端口证据见attention-row-proposal归档。
