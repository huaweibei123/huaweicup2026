# P3 统一结构算法：全100图×1–5核交接

本轮交给已获用户授权的集中执行会话 `nikolastarx/s-59ee5b053e1c48af8a64bc9ddb6ed5bc` 单写生产结果。算法源码的完整提交、500个坐标、分片和输入身份以 `results/a/q3-nikolastarx/unified500-handoff-20260925/` 的冻结清单为准。研发方保留src/q3写权，但不得修改已交接固定提交或并发运行同一清单。

**唯一算法入口是 `src.q3.adaptive_solve`。** 依次按原图结构守卫选择：严格5核/12 lane/四个524周期原算子的单迁移；普通重复阶段（沿用负载判别fixed/rotate）；闭合attention row与FFN同核；原guarded结构算法。没有case ID、成绩表、历史最优plan查表或按已测赢家手选。前三类均直接构造一案、在线完整E0验证一次；guarded最多评估两案，保留原计算下界剪枝和已评估anchor接纳规则。候选数有结构理由，不作参数网格或随机穷举。

生产前已有证据：stage旧三格、attention旧三格、attention+FFN的082/k4和单迁移051/k5分别实测。它们来自不同入口/提交，**不能替代本统一入口的正式500格**。整个新固定入口500格状态从未执行开始，旧成绩只作同身份对照。计算模型定理与静态证书不能填进官方Makespan列。

预算为至多500次solver启动、每格至多2次在线E0、总至多1000次E0保守预留；失败不自动重试。每格完整solver进程90秒，整项最多3600秒墙钟（队列/归档另报，不能重置逾期任务延长执行）；停止条件为该预算、用户停止、输入/源码身份异常或无法清理子进程。主清单与十个互斥分片二选一执行，不得两套均派发。单分片使用既有runner时采用原first-error-stop语义；集中runner如按格隔离失败继续其他既定坐标，须记录实际政策，不能重跑失败或擅自补候选。

并发由集中执行者在本机现有P1/P2窗口之间统一安排，先做4格链路检查（算入同一500，不重复），确认原件/调用账/进程组收尾，再按实际CPU/内存增加workers。每条记录报告实际环境、并发和完整进程墙钟；不能以多worker吞吐冒充单图时延改进。禁止与研发机制实验或其他已占用批次争抢。Colab未派发；如换平台另记runtime，不混成同环境单次时延。

全部原100图、固定config、未修改官方E0。先 `uv sync --locked`，从冻结附件取原始图并核对SHA，禁止修改raw。每次求解记录输入/源码、计划、完整官方result、receipt与E0调用账、完整stdout/stderr和子进程退出。成功、失败、超时、未派发全部保留，不将未跑记为失败评分、不把未知E0次数填0。

每核均值须给有效n/100；只有匹配固定官方单核原件的逐例 `B_i/M_i` 算术平均，没有分母留NA，不能用优化k1方案或总周期相除。全量比较统一algorithm/run，另列历史最优组合；同时报告M、额外COPY、spill、按字节Cache命中和求解时间。P3的同计划P2对照本批没有新增授权调用，缺失配对留NA，不把相对单核加速归因于Cache。

允许沿既有中央runner/导出器执行，但必须保留实际runner固定版本与不同于solver的身份、argv、候选账与终止政策。现有Q3反馈runner/导出器可作为可复现串行入口。原件及标准feed放本人独立结果目录，经预检、固定提交、既有签名outbox交方案成绩台并共享Fang；不得改写中央账本或另启服务。收到前几格的失败/明显退化/DDR异常后及时反馈本研发任务，是否修改算法由下一固定版本说明，不能本批热改源码。
