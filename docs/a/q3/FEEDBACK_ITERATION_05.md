# Q3 迭代05：重复向量通道的阶段归约

2026-09-24，运行前协议。机制推导见 STAGE_FORK_JOIN.md。

1. **目标**：解除重复 fork/join 图被旧弱连通分配器压在单核的限制，最小化官方 P3 Makespan；同时保留完整求解 wall、额外 DDR、spill、按字节 Cache 命中率。
2. **输入**：零评分结构普查识别出的 051、024、016；四核；冻结原图、官方源码与配置。分别为 24、101、305 个相同的 12 通道阶段。旧结果源 ba99b74b523f93a4002cd88970ec7164076d8008，仅复用，不重评分。它们是开发图，不称盲测。
3. **输出**：src.q3.stage_fork_join 的单次构造，原 ADD 树与算子保持不变；每个候选仅一次在线 E0，完整双字段方案及官方 JSON、逐次 ledger、调用与来源证据放 results/a/q3-nikolastarx/stage-20260924/。不以跨进程历史最小值冒充在线选择。
4. **限制**：总共最多 3 次新 E0、1 worker、每 job 90 秒、首阶段 T0 起全批 600 秒。先仅派 051/k4；其结果有效、改善且机制解释相符后，才显式派 024/016，沿用同一 T0、版本与额度。无重试，首异常停；若首阶段失败或不改善，剩余样本不派。不使用 E2、GPU 或 Colab，不新增单核分母或无 Cache 配对实验。
5. **验收**：先以合成原始 tensor/COPY 图检验端口守卫、原图不变、完整覆盖、合并 DAG 无环与归约跨核高度。这些是机制测试，不是官方成绩。运行需固定源码，官方结果与方案 SHA 逐字节检查；解释静态下界与 E0 的差额、实际缓存/搬运。全3格也不是全100图或全核数验收。
6. **后续**：若机制有效，交成绩台入榜并提出下一份具体覆盖计划；若失败，保留失败证据而非改参数重试。新 Pro 的封闭子树团队 DP 是独立待验路线，不与本批混用。

本机仍有其他 session 的任务。使用独立 detached 执行 worktree 固定本批源码，让主研发 worktree 的 Windows runner 修复与聊天归档并行；不改变他们的源文件或进程。只允许此批一个 E0 子进程。每次从启动读图到合法输出落盘退出均计 solver wall，在线 E0 含于其中，外层 batch wall 另列。
