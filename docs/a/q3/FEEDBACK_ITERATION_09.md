# P3 迭代09：封闭 FFN 同核的单格证伪

冻结一次机制检验：原082/k4、固定官方配置、`tree_solve --tree-method attention-ffn`。基于迭代08官方时间线，旧21个FFN中19个跨核，36个真实transfer载荷282624 B；这不是关键路径贡献的可加和分解。本轮仅增加原图闭合的 MATMUL→SIGMOID→MUL→MATMUL 四节点同核约束，保留首MATMUL→MUL旁路；输出仍原singleton，末端ready重排允许不同FFN和M/V交错，不融合/复制算子。

守卫包括原tensor非末端输出没有外部计算或COPY消费者、四个不同原op、M/V/V/M、attention row之外、不重叠和商图无环。pack_ffn=False默认输出六组合成plan/meta字节与80fab一致。静态082原4113计算节点完整覆盖；21组同核，内部跨核payload代理为0。新严格L500=166910，旧166318；新placement代理186474，旧183616。较差代理不能隐藏；166910仍低于旧官方183595，故一次完整E0有证伪价值。

预算严格1 solver/1在线E0、单worker、单例90秒、整批180秒，0重试、0新P2/分母/E1/E2/云。同身份旧官方plan/M183595、新增调度搬运3557904 B/spill0按固定80fab源和b934eb产物重用；不重跑旧方案、不在线择优。报告新M、求解完整进程wall、实际COPY/cache/spill，以及与旧值的质量—耗时取舍。失败或退化也保存；本协议不自动授权下一格或参数扫描。

源码先固定再在已收尾的本人稀疏执行树运行；必须等P1微图机制窗口明确释放。完整原件、调用账、源码/输入哈希独立目录，先审计再签名入台。若成功，下一阶段再确定统一结构路由，并把未测坐标交已获用户授权的集中批次执行会话；不能把这单格当全27图或全500保证。
