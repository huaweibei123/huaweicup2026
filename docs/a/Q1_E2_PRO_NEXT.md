# Q1：E2 接入判断与新的 Pro 数学问题

2026-09-24。本次是状态核对与针对性研究提问，没有新增算法成绩，也没有改动当前 Q1 评分入口。

## E2 可以帮助哪些搜索

已读取固定 `f4ee4756fc15c65ddc4256f3c73ff4efc89accfc` 的 `research/a/e2_search/README.md`、`HANDOFF.md` 和实际 `pipelines.json`。该开发版用于 P1：保留官方局部编译，用 C++17 逐事件重放全局过程，省去每候选完整诊断对象的构造；不属于旧版低精度近似器。完整诊断以及原生域外输入回退 E1，正式成绩继续 E0 确认。

开发者记录的四个 64 候选池，在 256 调用/248 个不同图–方案上三个返回指标与 E0 无差异。固定划分评分 API 相对旧 E1 `5bfe53a` 为约 26–30 倍，八种划分混合池约 5 倍；这些不含候选生成、文件 IO 和解释器启动，也不是与新原生 E1 的比较。实际 E2→E1 前八名→E0 的两个循环分别为 4.69 倍和 2.58 倍，仍不含生成/文件 IO。**以上是开发者交付证据，本 Q1 会话未重新测得，不能写成当前求解器的加速比。**

当前 Q1 的 `search.py`、`profile_refine.py` 仍使用固定 E1。后续适合的接入方式：

1. 同一划分下成组生成不同核归属/核序，使用 E2 评分，利用编译复用；先保留 E1 对短名单的完整复核及 E0 最终确认。
2. split/merge 改变划分后重新编译。不要按旧 Task 成员局部命中缓存；少量候选或编译主导的工作负载未必有相同收益。
3. 接入实验记录原生/回退路线，比较同一候选池、同样资源和包含生成/启动/复核的总成本，再决定是否改变默认路径。生产接口与共享原生内核由 evaluator 专项维护，本会话不修改其写范围。

## Pro 对已实现 Q1 的作用

- **直接构造来源：**第一路的非分叉链包、联合 Task DAG 的合法 cover 合并与出口保护进入了独立实现。当前并未采用其 3×3 max-plus 矩阵替代官方局部时长；不能把仍未实现的部件列为已有收益。
- **本机落地证据：**初始结构试验中，002 链包 119687、合并候选最佳 92089；008 链包 369908、结构候选最佳 113686。051 保护合并 342417、不保护合并 468368，说明必须保留风险识别与精确选优。这是结构方案的实际结果，不是对 Pro 智力贡献比例的因果消融，也不是同预算强基线排名。
- **数学与语义边界：**前三路帮助区分计算商图、完整 Task 顺序、Q1 与 Q2/Q3 的自由度。第四路补充的完整 Step1/COPY 区间、backing 与 incarnation 审计，促成真实剖面生成及反例回归；没有直接交付一个已被本机证明强于当前 Q1 的通用构造器。
- **尚未变成收益的部分：**Q2/Q3 阶段编排不直接迁移到 Q1；零 spill 不作收益保证；入口提前量排序在三个新池无收益，已暂缓默认启用。材料可读和思想有启发不等于所有定理/自报数字已验收。

## 已发出的新数学问题

第一路会话：<https://chatgpt.com/g/g-p-6ab2d820c86081918067a0c6d5eb1ab6/c/6ab2d851-0348-83e8-ab24-02db57b01aea>。

消息 ID `e03ba69e-151c-4b48-af23-499aa399441f`；界面选择 `6 Pro`，发送后明确出现 `Pro thinking`。逐字提示词及带 UTC 的发送回执保存在 `results/a/pro-q1-math-followup-20260924/`，阶段为等待回复，禁止重复发送。四个材料入口均为已同步的 Vioano 固定提交 `bb4c25d...`。

问题围绕直接生成有价值的 Task 二分候选，而不再只重排生成器已有的两个前缀：

1. 对 Task 内依赖向下闭合的成员集合，拆成同核相邻的两 Task，何时足以保持完整增广 DAG 合法？
2. 能否由外部输入释放标签构造阈值理想集；计算复杂度、覆盖损失和支配条件分别是什么？
3. 精确的 spill 前边界 COPY 字节集合函数，能否与可提前计算工作组合成最大权闭包/有向超图割？要求处理一生产者多消费者、输入重复读取及外部消费者；不能以该代理替代全局 makespan。

要求给证明或最小反例、伪代码、复杂度与最小证伪实验；允许否定所提路线，没有要求重复长综述或全 100 图搜索。当前没有收到新结论，更没有据此改变算法。

完整 `AI chats/` 历史目录由协调会话单写，本目录仅是增量问题与发送证据。当前页面只挂载六个消息节点，且内置浏览器的通用导出命令不支持，故没有宣称取得新的完整网页 clip。后续由协调归档流程按消息 ID 追加快照及新回复，不覆盖历史原件。

任务六字段：目标为确定 E2 的 Q1 用法并解决候选覆盖数学问题；输入为固定 E2 交付与 Q1 已验实验；输出为接入判断、贡献梳理和 Pro 在途问题；限制为尚未 Q1 实测 E2、Pro 回复待收；验收为源码/结果说明已读与发出正文/链接/Pro 状态回读；节点为数学研究已启动，非新算法性能验收。


## 后续状态：数学回答已收到

2026-09-24补记：以上“等待回复”为发送时快照，现已取得完整可见最终回答，消息ID `41db3ec6-5e9e-4692-b785-dee716020835`。没有重发。独立数学审阅、构造核和零评价测试见 [Q1_RELEASE_IDEALS_REVIEW.md](Q1_RELEASE_IDEALS_REVIEW.md)；E2仍未接入当前Q1默认评分路径。原Pro008两计划迁入A的官方对照见 `results/a/q1-q2-mechanism-pro008-20260924/REPORT.md`，不与作者P2数字混作同一场景成绩。
