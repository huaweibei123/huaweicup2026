# 论文验收台与整稿任务双向协作协议

协议版本：2026-09-27.1。用户明确要求验收台与当前论文组织任务互相同步标准、图件和人工批注，并按本协议执行。稳定事件、类别、覆盖、去重、作者响应与真实首轮样本按 [ANNOTATION_CONTRACT.md](ANNOTATION_CONTRACT.md) 和 `annotation-workflow.json` 执行；人和Agent读取同一份记录。

## 角色与固定通道

| 角色 | 实际任务 / 会话 | 单独负责的内容 |
| --- | --- | --- |
| 验收台网站维护与绘图协调 | `搭建论文可视化验收页面`；`01a0dc7a-4aad-77d1-aa6e-5d10c83d7ac9`；`nikolastarx/s-01a0dc7a4aad77d1aa6e5d10c83d7ac9` | 网站、Agent接口、已确定标准与批注的数据维护、图件持续接收与绘图任务分派或亲自制作、交付和复核状态展示、同步回执；不主动派新论文标注 |
| 论文固定稿Git发布与全队通知 | `总调度`；`01a0ccc4-9927-7003-b48e-b090380186b9` | 当前论文组织任务交出已冻结稿后，作为唯一发布协调者负责组织主库、镜像及队友通知；回交固定提交与实际回执，不修改验收台网站写区 |
| 当前论文组织、图件需求定义与PDF审核 | `排查并继续 Antigravity 论文工作`；`01a0dce8-2c05-7ff0-8fb0-e18a0c30aff5` | 操控原Antigravity会话，依据正文论证判断缺图、定义绘图需求、筛选最终版本、审核事实与逻辑、把Markdown编入现有LaTeX模板、每版生成并打开PDF供用户批注；本人不代写正文 |
| 原论文规划任务的历史材料交接 | `整理论文写作规划与图件现状`；`01a0da35-aa09-79c1-a2bc-058c06c60435` | 已停止同工作区的派工与正文写入；后续只向当前论文组织任务转交已固定材料，不作为新的批注/图件主路由 |
| 正文写作 | 用户当前Antigravity对话 `Verify CodeX Language Compliance` | 正文与语言改文的唯一负责人；接收固定原材料、标准、问题及旧新映射要求；交接接受状态须实际回执，不凭任务转告宣称完成 |
| 成员图件 | Fang工作台及实际改图会话，各以本人session签收 | 原图和图注写区，按同一标准回交；不同时改整稿或验收台 |

2026-09-26用户先把实际落笔交给当前Antigravity对话，后又将图件选择、材料组织、事实审核、LaTeX排版和PDF预览路由到上表的当前论文组织任务。原论文规划任务已确认停派工，只转交历史材料。本协议下文的“整稿任务”现在专指当前论文组织任务；正文修改由Antigravity完成。验收台不直接操作或另建Antigravity对话，防止两方同时指挥或写同一份正文。此前未提交CP03保留为历史工作稿，不是通过稿，也不要求新作者沿用。

两个本地任务用 `send_message_to_thread` 直接通知。团队共同原件保存在组织主库 [Issue 217](https://github.com/huaweibei123/huaweicup2026/issues/217)，涉及成员的通知带本人账号和实际session。直接消息与公开评论互相引用，不形成两套不同标准。用户后续明确要求优先；讨论或子Agent建议不自动升级为用户要求。

## 固定稿发布与验收台更新

当前论文组织任务冻结 PDF、源稿、图件和对应批注检查点；总调度负责组织主库与镜像发布并通知队友；验收台按已回交的固定提交更新网页及 Agent 接口。发布者每次须给完整提交、每个原件的仓库路径和 SHA-256、检查点号、改动范围、镜像状态及通知链接。验收台分别记录“本机已核原件”“主库有固定链接”“镜像已核”“队友已读”“论文已验收”，不能用一个状态替代其他状态。验收台和总调度不同时写论文工作区或同一 Git 发布分支。

网站旧逐项条目和语言位置当前仍绑定 CP01；后续 CP06 等固定稿先登记版本身份和发布状态，不能把 CP01 的页码、问题数量或通过状态搬到新稿。[检查点状态原件](checkpoint-status.json) 与 `GET /api/v1/checkpoints` 供人和 Agent 同读。CP04至CP06 PDF 已按 SHA-256 核对并由总调度回交组织主库固定链接；CP01至CP06属于第一轮v1；v2已固定发布，v3已按固定提交发布并核对原件哈希。旧批注不随版本名迁移。本机 127.0.0.1 页面只证明本机可读，不代表全队可访问。新稿须按原文另建审阅位置。

## 新稿与人工批注的固定流程

Antigravity写核心文本与Markdown → 当前论文组织任务按现有LaTeX模板生成并在Codex内置预览打开PDF → 用户选择典型错误批注 → 论文组织任务把原话和共性要求回填项目规则并交Antigravity全文修改 → 论文组织任务审核并将记录交验收台展示。两个Codex任务均不替Antigravity改正文。`paper/WRITING_HANDOFF_AGY.md`与`paper/AGENTS.md`是已固定的交接入口；验收台读取固定交付，不覆盖这些文件。检查点04当前PDF位于论文组织任务独立工作树的`paper/manuscript-v1/checkpoints/checkpoint-04-live/anonymous-paper-v1.pdf`，后续检查点另建，不覆盖用户正在批注的PDF；仅路径报告不等于已验原件哈希。

## 用户在验收台任务提出标准时

1. 验收台记录用户原意、适用范围和可检查要求；将确实新增或改变的要求写入 `catalogue.json`、`LANGUAGE_STANDARD.md` 和 `STANDARD_HISTORY.md`，发布固定完整提交及标准版本。
2. 验收台在本次工作回合内主动发给整稿任务，说明新增条目、替代了哪些旧要求、影响哪些稿件或图件，并给固定链接。影响队友时在同一共享Issue同步，不等用户再次转述。
3. 整稿任务实际读取后，回复所读提交、对规则的理解、受影响范围及下一步交付。只收到消息但尚未读文件时明确写“待补读”，不能写成已执行。
4. 无歧义的新用户要求立即约束后续修改。未同步完的事项不能被标成已验收；不受影响的工作继续，不用同步等待阻止整篇推进。

## 用户在整稿任务直接做批注时

1. 整稿任务在下一次对外消息中、且不晚于交付相关改稿时，将批注主动发给验收台，不等验收台询问。截图批注或PDF标注先保留原件；用文字说明其准确位置和含义。
2. 每组批注提供：批注ID、源稿提交、PDF哈希及页码或源文件行号、原句、用户原话或明确标注为转述的内容、作者理解、拟修改范围，以及是否影响已有标准。原话、作者解释和AI建议分别列出。
3. 验收台回报实际读到的批注范围，将人工问题关联到已有验收项；只有用户确实提出通用规则时才升级标准。缺少原句或位置的转述先登记来源缺口，不冒称已看过原始截图。
4. 验收台把新通用规则的固定版本发回整稿任务；整稿任务回读。修改结果逐条引用原批注ID和新位置，不能只回复“全文已改”。

## 共同记录的最少字段

用户通常用一处批注指出一整类问题。接收方必须记录具体实例，并提炼它背后的共性要求、说明适用边界，再同步另一方；不能只改被圈的一句。两方按“收到实例→提炼共性要求→同步双方→全文检查同类问题→回报修改范围与剩余问题”执行。相同问题可能采用不同词语，不以搜索原词代替语义检查。

例如：“硬件语义”对应概括词代替具体规则；“同核子图”对应省略对象关系的压缩表达；“执行子图”对应把表示方式误当执行对象；未交代循环等待就开始无环证明，对应缺少问题、风险和论证动机的铺垫。归类不是添加未经用户要求的新标准，应指出与原批注的共同缺陷，保留科学上必要且已解释的简洁表达。

作者回交时同时列出原实例怎样修改、全文同类位置怎样处理、实际检查范围和未解决项；不以一处改好关闭整类问题。

用户进一步明确要求由AI把单个人工发现扩展为全文专项检查。例如“执行子图”的检查对象是谓语与宾语是否匹配：硬件执行的是子图代表的计算任务，句子却可能把表示对象写成了动作对象。应逐句还原谁对什么做什么，查找词语不同但语法或语义关系相同的错误，必要时包含主谓搭配、抽象对象与实际动作的混淆。一次实例可能关联很多位置，但实际数量由逐句检查决定，不预设一百处、不凑数；必须交完整位置、具体理由和未审范围。

每个问题保留稳定ID、来源类型（用户原始批注／写作任务转述／AI提议）、原稿身份、原文位置、原文、问题、适用标准、责任人、改文身份及位置、待核事项、消息链接和当前状态。

状态分别为：已发送、接收者已读、作者已提交改文、验收者已回读、仍需修改／已接受。接收者亲自提供的回执才能记录为已读；HTTP成功、push、通知投递和作者自评均不代替回读。双向同步是否完成，应按具体标准版本和批注组核对，而非一次“收到”永久有效。

并发新消息按其用户要求的先后和明确替代关系处理。发现冲突先明确列出两条来源；只对真正不能确定的用户意图作澄清。已明确替代的旧规则及时标记失效，但不改写历史原句。双方每次交付前检查最新标准、未回执批注与待处理ID。

## AI初审与作者修改

用户最新分工下，验收台停止主动派新论文标注，已完成的初审与核对结果保留。当前论文组织任务负责监督Antigravity整改、组织全文同类检查及必要复核，并将具体记录交给网站维护者。网站接收和展示记录，不把收到改文等同于审核通过。若监督方使用子Agent，子Agent只提出意见，须由负责审阅者核对原句、上下文与理由；误报保留为不采用，科学含义不明时要求依据，不能把模型输出或没有命中关键词当成通过。

作者保有科学含义和修改的解释责任。遇到标注误解原意，按ID给定义、原题或源码依据，由验收台处理争议，不能静默忽略。涉及新的证明或实验需求应另列，不因语言整改擅自启动实验或扩大预算。

## 冻结版本与多人边界

供用户标注的检查点02已经冻结，PDF哈希为 `fb9165cb9a2b8dcb3ea5c59f05120b711919371da018f1f7d0b661d7a1875b3a`。改文进入后续检查点，不能原路径覆盖用户正在标注的文件。新源稿、旧TX-ID、页码和批注位置通过映射关联，不能静默串用。

正文和图中写法分别遵守用户最新要求：正文通常写“核心”，图中可用Core；首次给准确英文原词及对应关系。此前“正文Core必须保留英文”的约定被此次人工批注取代。图是计算任务的表示，描述执行时应说明相应计算任务或操作，不把图本身当作硬件执行对象。

## 图件接收与绘图任务路由

文字较多的流程图按 V03 与 L01—L13 检查。当前论文组织任务可让其负责的 Gemini 先对固定源图逐个文字单元提出“图内保留短标签／移至图注或正文／拆分”的改写清单，核对论文语境和科学含义后交验收台。验收台负责在自己的绘图写区修改可编辑图源、重新导出并目视检查；论文组织任务负责把相应完整说明写入正式 LaTeX 图注或邻近正文，并核对入稿版。两方按稳定图号、图源提交与文字单元 ID 回交，不能仅凭模型建议删除定义、条件或例外；图内短标签仍须符合中文论文表达习惯。

当前论文组织任务以正在形成的论证为依据，负责发现缺少的图、提出需要回答的具体问题，并给验收台一份有边界的绘图需求：论证目的、图中应呈现的对象和关系、计划放置的章节、依据的固定算法/数据版本、优先级、期望交付格式和可编辑来源。尚未明确数据或科学结论的图先标记待核，不用画面填补证据缺口。

验收台持续接收队友实际交来的图，登记固定来源、文件校验、提交者与实际作者的不同身份、算法及数据版本、图号候选、语言/科学/版面审核状态。收到具体需求后，验收台负责向合适队友分派，或在自己的写区亲自绘制；分派时明确唯一写范围、交付物和回执位置，不替队友确认已读。验收台把候选图及未核事项回传当前论文组织任务；后者决定是否适合正文、选用哪个版本、最终图号和图注，并反馈实际入稿位置。收到图、画好图、网页能打开、组织任务收到通知、入稿和科学验收分别记录，不能合并成一个“完成”。无需提前为尚未提出的缺图需求派工。

用户要求优先接收和展示由Fang从farmer、LYX的候选中明确选出的版本，或Fang重画的版本。只有可追溯的Fang选版/改图记录才获得这一展示优先级；仅在Fang分支提交、或Fang曾审过某张旧图，不能推断他已选中当前论文版本。原始作者与选版、美化、重画者分别署明。选优不代替当前固定算法与数据的科学核对，旧v4图即使已获用户确认成图，也不能自动进入新834d算法正文。Fang忙于既有图件审查时，不向已有两张在途任务的成员追加第三张并行图；验收台可先制作独立可编辑候选供其比较。

图件画廊按“图 章.节-序号”显示，例如“图 4.1-1”；同一逻辑图的原稿、改版和版面预览共用图号，分别标明版本。画廊编号为暂定定位号；当前论文组织任务按实际章节位置、论述顺序和选定版本核定论文最终图号与图注，回传验收台更新映射。固定来源、提交者、实际作者、选版和审阅状态分别记录；不能因为有图像预览或提交分支署名就判定图已用于论文或通过验收。[队友图件清单](team-figures.json)与 Agent 接口 `GET /api/v1/team-figures` 给出实时数量及同一来源清单。Fang图4-1仍对应旧v4算法；用户从Fang三版中选出的图4-2 A版已固定交付。论文组织任务在2026-09-26回报：该图可用于第四章基础构造的三支分叉分核说明，但不可冒充X/Y/J单支改分配的证明示意；图注和最终人工验收仍待审。验收台独立候选另列来源，不冒称Fang选版：现行834d流程图已有可编辑Draw.io和PNG，按论文组织任务反馈另导出删去内部状态尾注的候选，供Fang比选；case_026实例的X/Y标签修正版保留原图，其余像素未改变。这两份已在v2分别排于第19和20页，科学与最终人工验收仍待，Fang比选另记。Fang新转交的图4-3是用户选定的farmer原稿，图中case051/k3仅为单用例诊断；验收台已登记原图与纸宽预览。论文组织任务拟将它用于v8附录D并在正文交叉引用，实际入稿及新稿审核待固定PDF回报。

用户确认的图5-4与图6-5由Fang以工作台 `local-layout-v1` 版本原字节交付；其中包含此前获授权的布局修正，不得用较早的farmer父版替换。验收台将两张原图及165 mm纸宽预览登记为独立图件，画廊图号仅为临时定位号。图5-4的单例负结果与完整500格统计属于不同批次；图6-5的三个整体数是模型值、研究界和官方结果，并非三种算法成绩。用户选图、工作台逐图验收、队长科学与语言审核、整稿实际入稿分别记录。整稿任务实际读后决定本版v8暂不插入图5-4/6-5：正文已有另一反例及问题二全量图，图6-5对应的条件研究界也尚无正文推导；保留原稿供后续章节适配，不将暂缓用途误写成图件科学否定。

图件清单在每次网页或 Agent 接口请求时重新读取；维护者登记新的固定交付后，浏览器刷新即可看到新版本，不需要重启本地服务。远端图像仍按登记的 Git 对象哈希和 SHA-256 校验；尚未公开的候选不会被误报为已送达。

2026-09-26 Fang另交图5-1与图6-1用户确认A版，固定提交84a327a79d70259d03f5a7e445ba898721f7c310，两张PNG已按字节和哈希回读并在画廊优先展示；单页预览、原作者与美化者分别登记。它们尚未插入v6整稿，科学、语言和最终版面审核待论文组织任务回执。

2026-09-26论文组织任务回报已目视并按固定算法核对：决定在v2采用Fang图4-2 A版的三支分叉示意、去内部状态尾注的834d流程图、case_026 X/Y修正版；v2实际图注、数值与LaTeX实排已回报并登记为入稿；语言、科学与最终人工验收尚未通过。第二、三问三处补图已制作成可编辑v3候选；它们不在已冻结v3稿中，待论文组织任务科学核对、Fang比选与下一次明确的入稿决定。

Fang、整稿和验收台各在自己的写区操作。规范通知应转交实际改图会话并收各自回执，账号级回复不代表全部会话。无需为同步开启后台进程；本协议通过实际工作回合内主动发送、逐组回读和交付前检查执行，不声称离线会话能被实时唤醒。

## 本次落实

验收台负责发布本协议并直接通知当前论文组织任务；对方实际读取后给固定提交回执，并向当前Antigravity写作负责人转交。检查点02的17项人工批注已按固定提交`e34ded176`逐项核对选中文字、用户原话、页码与矩形；这只证明监督任务归档原件与验收台记录一致，不等于本任务独立看过原始截图，也不等于改稿通过。双方回执保存在共享Issue与 `handoffs.json`。任何尚未回读的一方继续显示待签收。
