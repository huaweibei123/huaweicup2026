# P3 统一结构算法全量 benchmark

负责人：@NikolaStarx；执行会话 `nikolastarx/s-59ee5b053e1c48af8a64bc9ddb6ed5bc`

1. **任务目标**：为固定 P3 求解器 `src.q3.adaptive_solve` 建立同一算法的 100 个官方图×1–5 核完整运行证据；不拼旧机制样本或历史逐格最优。
2. **输入文件**：算法源码提交 `2d5459fa042507cce0f0343e544ed43784c8488b`；交接材料提交 `36e9fddc2aece856112ef5faab870e4ca7e91099` 中十个互斥50格 manifest；原始图、配置及官方 E0 由冻结 `docs/a/source-manifest.json` 校验。官方单核分母引用既有固定结果提交 `ba99b74b523f93a4002cd88970ec7164076d8008`。
3. **输出要求**：`results/a/q3-nikolastarx/unified-full500-20260925-s59/20260924T1741Z-s59ee/` 包含调度收据、每分片完整原件、逐格计划/官方 result/内部 E0 账/运行收据，以及聚合标准 feed。统一批次名 `q3-unified-full500-20260925-s59`；原始分片 run_id 在各自 manifest 和每条记录参数中保留。
4. **限制条件**：最多500 solver、1000次在线官方 E0；单格完整进程90秒，单分片360秒，整项3600秒；0重试、0 E1/E2、无新增P2/单核求值。先并行四分片并核对四个指定首格，再最多8分片同时执行，派发前可用内存至少6GiB。固定算法各结构分支自动按输入选择，在线 E0 与回退都计入求解墙钟。
5. **验收标准**：500格全覆盖、同一solver SHA/入口、真实求解和 E0 调用账；成功结果由冻结官方E0在求解内校核、完整原件与单核分母均按SHA读取；每核逐例倍数算术平均的有效数须100/100。协议校验与成绩台入库分别回读。
6. **截止时间**：用户未指定日历截止；本地空闲评分窗口已于2026-09-24 17:40:16 UTC启动。

实际运行：macOS M5 Pro、Python 3.12.13、`uv sync --locked`；执行 `.venv/bin/python -B scripts/s59ee_p3_shard_dispatch.py --batch results/a/q3-nikolastarx/unified-full500-20260925-s59/20260924T1741Z-s59ee`。全项于17:44:29 UTC结束，十片均完成、500/500成功、500次solver和517次官方在线E0，0 E1/E2、0重试。最长单格完整进程29.95秒。

结果：同一算法相对冻结官方单核的逐例倍数均值，1–5核依次为1.135796、2.164724、3.020554、3.776003、4.381547；每项均n=100。该倍数不是P3的同核数无Cache配对增益；本批未新增无Cache配对，缺失留NA。官网结果代表模拟Makespan，未验证真实硬件收益。

待验证：成绩台中央实际接收、独立平台复跑及同核数P2无Cache配对；本任务不要求新图表。固定提交和接收回执随数据提交补充。
