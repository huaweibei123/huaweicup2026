# P3 第三轮：理论极限、剩余空间与迭代方向的证明审计

负责人仍为 yuanzhifang30-sudo/s-3d9c78db26714786b88b987ca6f58e2b。本轮为论文理论咨询，不开启新一轮算法评测。请使用最高 Pro 档，审查证明，不迎合“必须证明正在逼近最优”的预期。前两轮的原件、失败和勘误继续有效；本包是新增的固定材料。

请先解压附件，读 START_HERE.md 和 MANIFEST.json，核验关键文件大小/SHA，并明确哪些文件实际全文读到、哪些只解析字段、哪些未读/不可访问。主库现已公开，但链接存在不等于已读；优先使用包内固定字节，外部补读使用完整 SHA 链接。不要读取或输出隐藏推理；只提供可公开审查的证明、推导和反例。

## 1. 正确界定“理论上界”

官方首要量是最小化 Makespan。请令合法两字段方案集为 F_3(G,k)，M_3(P) 为冻结 E0 的周期数，M_3* = min M_3(P)。当前合法成绩 U 是 M_3* 的上界；要给性能上限，须证明对所有合法方案有效的下界 L <= M_3* <= U，然后换算固定单核基线 B 下的最优加速比 S* <= B/L。明确严格全局界、受限算法族界、固定方案界、含额外模型假设的界、启发式估计各自量词。

不要混淆三种“空间”：绝对最多改善 U-L；相对当前时间最多降低 1-L/U；当前对最优的最坏乘性证书 U/M_3* <= U/L。它们都不是已证明可达到的收益。逐例加速比取算术平均；不能用总时间比、最优组合拼盘或平均U/平均L冒充官方均值。

P3 还必须另报同计划 G(P)=M_2(P)/M_3(P)、总额外 DDR、spill、按字节 Cache hit 和完整求解墙钟。G 的分子也随方案改变，不能为了提高比值故意恶化 P2；固定分子的 B/L 不能直接移植为跨方案 G 的上界。请分析若要给 G 的理论上界，须增加哪些条件或改变比较定义。

## 2. 冻结语义与资料

输出只能 node_to_subgraph、core_schedules；不能提交操作时间戳、任意 Pipe FIFO、Cache钉住/预热策略，不能改原图、配置、树或计算操作。P3 每核重建一个 Task，Step1/稳定分桶、Step2 spill、Step3 FIFO/内存复用依赖、最终跨核500和共享服务均来自固定官方源码。L1=524288 B、UB=131072 B、Cache=1048576 B，DDR=60 B/cycle、Cache=250 B/cycle。逐笔服务量先取整，共享服务推进仍有浮点/退休取整语义；有理数流模型的证明不得无条件升级成官方实现证书。

包内包括原题、全部100图原ZIP、官方10份源码/config/manifest，固定统一算法源码和500行feed，旧全100计算松弛界的500行bounds及其审计说明，本会话当前论文与证据索引，以及044部分预加载、最新query-flow研究的固定证据。所有来源版本见清单；不要把论文草稿中截至旧时点的“尚未运行”覆盖后来的明确实验收据。

统一全500仍是 src.q3.forest_solve.main@311322b996c0948e8a6a9c7ec6ddfe6ae41fbee1，feed@19bebf35205d23fdd832781540f8879da52eeb62。五核 mean(B/M3)=4.7576166788448955，mean(G)=1.082917172693062。旧 global-bounds 报告引用的是更早成绩，不能沿用其中历史当前值；本包 current-gap 表把其固定 L/B 与新500成绩按图hash/config/official身份连接，属于算术更新，未重跑官方。请独立审查该 L 的证明与数据一致性，再决定是否能作为证书。

后续单格：044/k5 全前缀38024 -> 部分预加载37060 cycles，下降964；两计划分别同计划P2=M3，所以G均1。hit=11264 B、miss=1012064 B、extra=140800 B、spill=0均未改善。新计划0a75e3613ad5f69e293e45e6c1cfc1545b3b1036245ebc7bf0af75b3321df0fd。它是机制单格，不能替换统一算法主表或声称冷求解速度。原固定key多重集的首次miss上界已饱和，不能据此证明所有合法计划的Cache全局上界。

067/k5历史当前12237901；完整job归核家族下界12446880已排除该家族。此前11856672/11857220等为指定阶梯顺序的必要界，不能作为全局最优值。最新071/069 query-flow把同候选计算FIFO+500界降至4538/5846，尚不能由下界下降推得官方M下降；本包固定发布点该单格预算尚未运行。R8关于阶段单调和物理支持容量的独立收紧见 REVIEW.md。

## 3. 必须交付的数学结论

A. 从官方原语义推导可机械校验的下界体系。先验证 CP、各Pipe总工作/k、最大操作这组全局松弛；再研究如何加入不可避免的首末COPY、容量/IO、跨核通信、共享带宽而不错误限制合法分核。哪些量必须min over计划、哪些只能用于固定方案？可用割/流、LP对偶、max-plus或其他工具，但请给完整量词、证明、计算复杂度和反例，不能只列名词。没有一般强界时，明确不可用的理由，保留弱而正确的证书。

B. 用固定100图与500行真实成绩给出逐例/逐核剩余空间表，并给1–5核 mean(B/M) 的上界与差额、最值得改进的图对全100均值的最大贡献。对全局有效性未核实的界不要算成严格剩余空间；未知单列。044部分预加载等新单格单列，不能混入统一500。尝试加强至少一个代表例的界；如果证明缺条件，给出缺条件与应补的最小证据。

C. 对迭代路线作“数学进步审计”：结构识别/连续划分DP/容量与生命周期/森林峰值排序/相位阶梯/max-plus释放包络/部分预加载/跨Attention query-flow，各自到底优化什么受限目标，有何交换论证、最优子结构或安全编译不变量？哪些只是代理下降？特别审查减少COPY、提高hit、降低固定方案下界能否推出M下降；使用已知负例，必要时给最小反例。

D. 给出能够支持论文的有限而准确的“朝性能上限优化”命题。对同一输入、官方身份和有效固定L，若每次仅接受合法且E0更小的方案，U_t单调下降，认证间隙U_t-L如何变化？若L_t也改进且均全局有效，如何构造嵌套证书区间？这是否仅为单调证书改进而非收敛到M*，为何？严格指出缺少何种完备邻域、近似比或终止证书就不能声称全局收敛。局部模型导出的候选方向如无全局保证，怎样仍给出可证的结构解释和可证伪机制假设？

E. 为随后P1/P2/P3的第四个综合Pro咨询准备一个可组合接口：三问共有的原DAG、算子原子性、资源/事件图、提交编译、合法性、松弛证书、可行构造、官方验证层；P1到P2到P3哪些语义真正变化，哪些结论能复用，哪些不能宣称可行域简单嵌套/Cache必有收益。给出共享符号和“假设—命题—证明边界—证据”的对照。不要凭缺失的P1/P2新答案杜撰三问共识；本轮只给P3可输出的接口与待交叉核对问题。

## 4. 输出与执行边界

请给可直接审阅的中文理论说明（定理、证明、适用条件和反例分开），表格和完整可复现的只读证书/算术脚本。必要时提供CSV/JSON/Markdown附件，同时在正文保留关键公式、数值表和结论，避免附件不可取就失去依据。公开说明实际运行的静态脚本、输入、结果与未运行范围。

本轮允许读取、静态遍历、哈希、证书核对和小型解析反例；不要调用求解器/Task构造/Step1/2/3/E0/E1/E2，不做候选枚举/随机搜索或大型精确求解，不把作者脚本运行算作本机成绩。最终列出可直接写入论文的已证明结论、需要本地独立核验的结论、被推翻/需收紧的旧说法，以及最多3项最小证伪建议；建议不等于已执行授权。
