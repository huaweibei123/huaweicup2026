# 初审：从 Pro 建议到可证伪对象

本机 2026-09-24 初审，源图/官方源码冻结身份沿用 `161cdb35de11b0d174a5a0ca149aa36657af2abd`。附件及作者实验原件保留，未把其成绩当本机复现。

## 已经核对

- 整体回复 2 条消息，最终回答完整复制；原提问与已发送正文一致。ZIP 72 成员及作者71项哈希均通过，单独报告与包内报告一致。
- 返程错位构造只输出 node_to_subgraph/core_schedules，不改 op ID。严格独立链同核的分组使数据边及核序边沿波次前进。P1 仍需前一整个 Task 结束及100周期门控，重叠发生在混合 Task 内。
- 008原始 ZIP 条目独立取得，SHA256 `c93bb7ab5deec5112aff0cc001fbd76d001d3de5ea7463fba59b1f1ff2ba3e1d`；作者保守识别器确认108条同构 M→V+→M 真链，a=c=1158、b=2196。独立逐 tensor 重算全部108链的每个切口，均与作者界面表一致：18432/18624B，最低额外独占DDR服务616周期，必要外部DDR服务44604周期。
- 重算的四核“保链阻塞/拆链搬运”条件下界85590（最小化发生于66条拆链）；不拆链的受限下界121824。二者适用范围不同。这个下界仍以完整链在任意混合Task内的Step1连续计算块定理为前提；初审正在独立补核该全称前提，不能用观察一个候选FIFO替代证明。
- 同理给定62738时要求最小切口不超过5100B的必要条件，在008实际18432B面前不满足；这说明旧弱下界的大差距并非全部可实现空间，不证明123060最优。

命令：`python3 src/review/q1_return_cut_initial_audit.py --output results/a/review/p1-pro-initial-20260924/008-independent-interfaces.json`。此命令只作图解析/整数证明参数核算，solver/E0/E1/E2均0；109个拆链整数是下界优化，不是调度计划评分搜索。结果含输入/作者源码SHA及静态耗时。

## 仍须核验

1. 未切链连续块定理须对所有合法Task混合成立；新prefix优先旧return也须核实际Step1，不能仅凭链形态套理想 F(u,v)。
2. 无MEM充分证书须核官方 VIRGIN额度使用规则。总驻留和无spill不能一般推出无MEMORY_REUSE。
3. DDR单位为每个COPY取整的服务量，不能把字节直接当延迟；包络未证明binary64/EPS所有输入边界。
4. 通用窗口下界需核必要jobs，不得混入候选FIFO/Task边/计划限定内存边。
5. 用6个固定合成E0（A whole/return/entry、B whole/return、C skip接口）核模型；1worker，30秒/例、240秒总、0重试、首身份/合法性/断言失败停。真实008只在审计通过后另冻结最多1候选，复用旧基线，无参数扫描。

公开归档与初审完成不等于这些定理、部署适配器或官方成绩终验；后续修正另存，不改原件。

## 初审已发现的限定条件缺口

独立审阅发现作者识别器允许“首M无输入、仅一个V”的链；两个私人链 M1000→V1500→M1000、所有产出60B、IDs为[1,2,3]/[11,12,13]时，混合Task的终端深度打平，官方Step1的ID顺序可让旧return先被访问。纯模型已得混合计算序[3,11,12]，M FIFO为旧C3→新A11，其响应3502而不是按新A→旧C假设得到2500。故不能由 recognize 成功直接套理想F公式；应检查实际FIFO/严格终端优先。作者Uenv已读取实际模型Step1，可能仍能保守回退；这不是上述cut-or-blocking下界的反例，也未运行官方E0确认此小反例。
