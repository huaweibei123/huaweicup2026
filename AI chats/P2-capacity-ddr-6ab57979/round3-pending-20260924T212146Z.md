# 第三轮已发送问题（答复尚在生成）

- 原会话：https://chatgpt.com/g/g-p-6ab540961238819181aa53e685fdb456-huaweicup/c/6ab57979-aa78-83ee-bc0d-da8c426d737a
- session=yuanzhifang30-sudo/s-eb28fa11a5664fdfbdd29b3d6e38ca24；仅 P2。
- 发送 UTC：2026-09-24T21:21:46.973Z；用户消息 ID：eb792ac9-cd9f-4417-948f-4e60d7123ce8。
- 发送前在用户指定项目确认 Pro 最高档；问题正文从原生公开消息读取，共 2138 字符。此文件只归档该提问，不冒充本轮已完成问答。
- 2026-09-24T21:39Z 仍见生成停止按钮，原生最新轮只有用户消息；read_thread 的 completed/idle 不当作答复已完成。已设本任务 5 分钟完成监听，并在工作中主动检查；最终答复另追加全会话快照，不覆盖本文件或前两轮原件。

---

继续本 P2 专项（不是 P1/P3 会话），请用当前最高 6 Pro，仅审计下面一个窄命题，寻找反例或可实现的修正，不另写大综述。

先更新已测事实：我们本机用未修改 E0 测了固定 F1 六格。008/095 k3 的 M 恢复84303/357591、spill=0；025/036保留gap成绩；072/014仍未获证并有spill。F1先判现有同构resource_word并用真实物理token闭桶容量证书准入，其余原gap路线不按图号择优。这不是你先前完整联合DDR队列FQ，也不是全局最优。已有gap全500的k5均值3.676575，低于旧tensor3.896037；新F1 k5尚未全测，不得填造完整均值。

本次瓶颈：希望用两个有结构依据的直接构造A/B及一个便宜门，消除对全局E0反复评分的需要。沿用本会话已上传 P2-capacity-ddr-evidence.zip、官方45f647b395b84e9569f418fd33d62c2b8eb4d190，code hash de11a83db8d7c47ed328b15a7df71d613a833b16cd23ee9fe877999578a1ace0，config hash dcd10de54b23f8366428fb24e828812b1da9549e6eae4a3c3f38604fe5ae77b9；L1=524288、UB=131072、B=60、跨核lag=500、每pipe一槽、每核一个Task。请先说明你此轮实际还能读取哪些原文件；读不到就明确，不凭上一轮结论代称读到。

候选命题：A为合法singleton计划，每个eligible弱连通分量完全在一核，P2重建cross_links=[]，无logical_tid alias，所有真实本地token闭桶证书通过（仅作为Step2无spill的充分条件），Step2/Step3/全局正常返回。令 C_c 为该核所有eligible操作按官方_op_duration逐个求和；W_A为P2重建基础COPY逐条 max(1,ceil(bytes/B)) 之和（0B COPY也算1，不把原始COPY节点重复计入）。猜想理想连续公平共享模型有 U_A=max_c C_c+W_A >= M_A。

我们的条件证明：取最终完成核c。c未完成时，若全局没有DDR操作在运行，则其无环本地data/memory/FIFO依赖链必有一个eligible操作运行；没有跨核lag或其他正等待。所有DDR忙时间总长至多W_A，其余时间可计入c自己的C_c。因此成立。请优先攻击这一步：Step3的memory free/alloc、MTE FIFO、原COPY归一化或全局发射规则会不会留下“无DDR且c无eligible运行”的正时段？仅有cross_links=[]是否足够，还是还需其他易检查条件？不要把需要真的调用Step2/3/E0才能知道的成功条件藏成免费检查。

另对固定合法B（原gap，可有spill和跨核link），L_B仅取B自己重建基础COPY的逐条归一化独占服务总和，不含spill。理想模型 L_B<=M_B。所以若U_A<L_B，可筛选A优于B；这不是最优性证明，亦不能将不等式失败反解成B更好。该COPY多重集希望只从图、核映射和输入/最终输出/producer-consumer核对/direct边规则O(V+E+I+k²T)计算，而不编译完整Step2/3。

真正难点是冻结Python实现：_advance_ddr用float及1e-9完成阈值，ceil(cursor-1e-9)投影整数事件。上一轮p2_bounds.py中“strict certificate”未经机器层误差证明不能直接保留。请审计：
1. 精确模型的U/L是否成立？若失败给最小明确反例；若成立给完整条件和短证明。
2. 对本题有限整数输入，能否构造成本低、显式可计算的ε_A/ε_B，使 U_A+ε_A<L_B-ε_B 对官方返回的整数M仍可靠？必须覆盖整数投影产生的空转、近并列退役、残余量累计浮点误差和最大事件次数。可以给保守但有用的域守卫（时长/字节上限、事件上限、核数），不要未经证明直接加1或加COPY数；若严格封闭不值得，明确降级为启发式并解释实际如何最小证伪。
3. 若此门有效，给一对固定候选的简短算法和拒绝/回退条件：如何构造整分量A并证明/检查本地无spill及合法性，B保留当前gap；不能查历史图号或成绩选优。端到端必须计入两候选及全部守卫开销，说明哪些部分需要真实计时。

020/045 k5保存的旧packet方案无分区搬运且spill=0，gap却分别拆271/288、68/68个弱分量；这只说明研究价值，不给你未上传图的结构或新成绩。无需针对它们编造数值。不要全量跑100图；若要反例实验，至多4个极小合成官方全局E0、明确每次输入/输出/调用成本。只交此命题、反例/证明与最小可执行门，不运行大图。正文尽量控制6000汉字内；必要脚本直接给完整小代码块，避免只交无法下载的ZIP链接。

